// DISABLED: Replaced by smoke tests in tests/integration/smoke.test.ts
// Complex constructor mocks (CircuitBreaker) are better tested via integration smoke tests.
import { describe, it } from 'vitest';
describe.skip('vertexAiService (disabled - se smoke.test.ts)', () => {
  it.skip('placeholder', () => {});
});

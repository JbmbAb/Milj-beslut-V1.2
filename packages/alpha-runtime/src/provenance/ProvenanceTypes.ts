import { HashDescriptor, RegistryReference } from "../types";

// Note: Re-exporting from types.ts to maintain backwards compatibility 
// with the pasted structure if needed, or simply forwarding.
export { ProvenanceRecord, ProvenanceGraph } from "../types";

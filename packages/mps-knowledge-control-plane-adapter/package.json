{
  "name": "@miljobeslut/mps-knowledge-control-plane-adapter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "KNOWLEDGE-CONTROL-PLANE-INTEGRATION-V1 -- pure, format-only composition between the Knowledge domain layer's own DEV-GOV unit definitions/vitest run outcomes and the generic Multi-Agent Control Plane V1. Never computes, infers, or overrides a PASS/FAIL verdict; only translates shapes that already carry one.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@miljobeslut/mps-control-plane": "file:../mps-control-plane"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  }
}

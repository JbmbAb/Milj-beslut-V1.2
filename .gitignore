# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/keybindings.example.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.secrets/
.env.test
.env.local
.env.*.local
.env.supabase
.env.production
.env.staging
coverage
docs/qa/coverage-baseline-generated.md
docs/qa/vitest-backlog.md
playwright-report
test-results
.quarantine/
figma-raw.json

# Temporary files
.temp_import_cache/
.tmp-*
tmp_*
tmp-artifacts/
tmp-mimers/
*.txt
*.json
!package.json
!package-lock.json
!tsconfig.json
!.prettierrc.json
!stitch.json
!figma-raw.json
!vercel.json
!*.md
!public/manifest.json
!tests/fixtures/**/*.json
!packages/mps-compliance/tests/golden/**/*.json
!packages/mps-runtime/src/verification/performance/baselines/**/*.json
!docs/ops/benchmarks/**/*.json
!docs/architecture/admit-v1/**/*.json
!scripts/alphaevolve/experiments/**/*.json
# P2-SR-RELEASE-01 — the GOVERNOR-signed production source authority.
# Tracked deliberately and by exact path: the runtime resolves it from the repo, so if it is
# only ever installed locally the harvest is not reproducible from a clean checkout. One path,
# no wildcard — a broader exception would silently pull in untracked local registry files.
# Its bytes are the signed representation; `*.json text eol=lf` above keeps them LF on checkout.
!source-registry/national-registry.json
# SOURCE-REGISTRY-RELEASE-HYGIENE-01 — superseded authorities, kept for provenance and replay.
# A quarantined object names the exact registry artifact it was acquired under, and that
# artifact may since have been superseded: the 144 objects harvested before the PUH size
# reissue resolve to reg-dv-puh-mmod-002, which is no longer in the active registry. Leaving
# these untracked would make historical verification depend on one machine's working copy and
# put them one `git clean` from gone.
# An explicit allowlist, not `legacy/**`: a recursive exception would make every future file
# dropped into that directory trackable by default, including unverified material. Each
# historical record is admitted by name after it has been classified. legacy/README.md needs no
# entry — it is not matched by the *.json rule above — and drafts/ plus the inventories stay
# ignored pending a separate retention decision.
!source-registry/legacy/puh-mmod-001-superseded.json
!source-registry/legacy/puh-mmod-002-superseded.json
!source-registry/legacy/geo-sources-superseded.json
!tests/fixtures/**/*.txt
!scripts/google-adk/requirements.txt
!prompt_optimizer/requirements.txt
!prompt_optimizer/requirements-dev.txt
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/keybindings.example.json

# Python cache
__pycache__/
*.pyc
.pytest_cache/
prompt_optimizer/out/
prompt_optimizer/data/
prompt_optimizer/.pytest_cache/

# AlphaEvolve local checkout (secrets + venv)
alphaevolve-on-googlecloud/
.venv-adk/

# Local agent/scratch (not source — Mimers Brunn policy)
.agents/
scratch_*.ts
seed-*-test-data.ts
task.md
*.bak
scripts/temp_*.cjs
scripts/temp_*.js
scripts/check_*.cjs
scripts/fast_*.cjs
scripts/find_*.cjs
scripts/inspect_*.cjs
scripts/list_*.cjs
scripts/test_discovery.py
scripts/db/generate-seed-gis-stubs.mjs
scripts/db/trim-database-setup.mjs
scripts/import/mimer-smart-*.ts
scripts/import/mimer-smart-*.cjs
scripts/import/promote-quarantined-svar.cjs
scripts/import/seed-mock-knowledge.ts

# Local debug artifacts
.codex-logs/
.postqode/
.gemini/
cred.err
cred.out
push.err
push.out
openssl-push.err
openssl-push.out

# Repomix context dump (autogenererad, ska ej committas)
repomix-output.xml
repomix-output.txt

# Runtime storage: uploads, ingest caches, extracted datasets (blob store / local disk — not source)
/storage/

# Playwright CLI local page dumps (not source — regenerate if needed)
.playwright-cli/

# Windows shortcut files (not source)
*.url

# Generated local analysis / dossier outputs (large GeoJSON)
Analysis/

# Local scratch, outputs, dossier builds (not source)
scratch/
output/
dossiers/

# Local raster/pyramid tiles (gdal2tiles → MapView ortofoto); not reproducible artefakter för CI
public/tiles/

# Ad-hoc pg_dump / migration backups (lokala)
backup_pre_migration.sql
backup-*.sql
backup-.sql
backups/prod/

# Lokala PostgreSQL-datafiler / runtime-datadir (ej källkod)
Database/

# Lokala AI/editor-artifakter
.kombai/

# Lokalt brus: examensmaterial, nedladdningar, IDE-sessions, tillfälliga Copilot-skript
.copilot-*.ts
*.session.sql
Examensmatris*.xlsx
Sammanställning/
downloads/
examensrepo/
examensarbete_*
kravmatris_*
mall_c_anmalan_*.md
hardware-check.ps1
hardware-check.ts

# Lokala sync-/träningsartefakter (ej källkod)
rclone.conf
rclone.exe
training/

# Stora geodata-rastrar och databasinitialiserings-dumpar
*.tif
*.tiff
docker/postgres-init/raster-poc/
docker/postgres-init/**/*.tif
docker/postgres-init/**/*.tiff
libsf_mini_core_windows_amd64.dll


{
  "report_schema": "phase-c2-disposition-v1",
  "program": "PUBLIC-PRISMA-BASELINE-RECONCILIATION-01",
  "phase": "Phase C2 -- cold, independent disposition of the 98 HOLD items",
  "generated": "2026-09-13",
  "verifier": "independent cold verifier session (no participation in prior Phase A/B/C)",
  "frozen_input_sha256": {
    "public-prisma-baseline-comparison.json": "7e0bcd73d758561418b32166e76d56dfda5f15b1b03ec96e5d26d3dc24b9226a",
    "public-prisma-baseline-ratification.json": "0b39643bcef44ba77aa7fd9c7581fbe39defba527ded1aa59c45334cfaeafca8"
  },
  "total_items": 98,
  "counts": {
    "COMPARISON_ARTIFACT": 25,
    "OWNER_DECISION_REQUIRED": 4,
    "TRUE_SCHEMA_DRIFT": 19,
    "NAME_TRUNCATION_EQUIVALENT": 50
  },
  "items": [
    {
      "object_identity": "relations: _prisma_migrations|r",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "relation \"_prisma_migrations\" (kind=r, persistence=p)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "relation \"_prisma_migrations\" (kind=r, persistence=p)",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "The relation is Prisma's own internal migration-ledger table, which schema.prisma structurally cannot declare by design; ACTUAL and HISTORICAL agree exactly, DECLARED is absent everywhere for this table.",
      "residual_uncertainty": "None material. This is well-documented, widely-known Prisma behavior and is corroborated end-to-end (table + every column + PK constraint + PK index all show the identical pattern)."
    },
    {
      "object_identity": "relations: localization_geometry_supersessions|r",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "relation \"localization_geometry_supersessions\" (kind=r, persistence=p)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "relation \"localization_geometry_supersessions\" (kind=r, persistence=p)",
      "evidence": "prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment states verbatim: \"2. localization_geometry_supersessions: append-only, non-authoritative discovery projection for supersession-artifact refs (same shape as project_assessment_projections / localization_geometry_projections -- raw SQL access, not a Prisma model).\" schema.prisma has no `model` mapping to \"localization_geometry_supersessions\" (confirmed by grep for @@map(\"localization_geometry_supersessions\") and for a non-Request-suffixed model name: no hits). ACTUAL and HISTORICAL are byte-identical for the relation, all 6 columns, the pkey + project_id FK constraints, and all 3 indexes; DECLARED is null throughout. This is a first-party, in-repo, dated design statement that this table is deliberately raw-SQL-only.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "The relation is a documented, deliberate raw-SQL-only projection table, explicitly stated in its own creating migration to not be a Prisma model. ACTUAL and HISTORICAL agree exactly; DECLARED is absent for the whole object graph (relation, columns, constraints, indexes).",
      "residual_uncertainty": "Low. The migration comment is explicit and dated; verified schema.prisma has no model for this table while sibling tables named in the same comment (project_assessment_projections, localization_geometry_projections) WERE later promoted to real Prisma models, which is consistent with -- not contradictory to -- this table having been deliberately left out."
    },
    {
      "object_identity": "columns: PostgisImportBatch|imported_at",
      "source_classification": "INTENT_REQUIRES_OWNER_DECISION",
      "actual_definition": "column imported_at type=timestamp(3) without time zone not_null=true default=\"now()\" ord=5",
      "declared_definition": "column imported_at type=timestamp(3) without time zone not_null=true default=\"CURRENT_TIMESTAMP\" ord=5",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "grep -n \"model PostgisImportBatch\" prisma/schema.prisma shows `imported_at DateTime @default(now()) @map(\"imported_at\")`; grep -rl \"PostgisImportBatch\" prisma/migrations/ returns zero files (no migration exists for this table at all).",
      "disposition": "OWNER_DECISION_REQUIRED",
      "reason": "ACTUAL/DECLARED agree on type and not-null but differ in default-expression text (now() vs CURRENT_TIMESTAMP); no HISTORICAL entry exists at all because PostgisImportBatch has no migration file in prisma/migrations (the whole table was created out-of-band). schema.prisma literally declares `imported_at DateTime @default(now()) @map(\"imported_at\")`, and Prisma's schema-engine is documented to compile `@default(now())` on a DateTime column to SQL `DEFAULT CURRENT_TIMESTAMP` -- so DECLARED's CURRENT_TIMESTAMP is exactly what running the Prisma migration engine against this schema.prisma would produce, while ACTUAL's literal `now()` must have come from a hand-written/raw creation of the table. now() and CURRENT_TIMESTAMP are Postgres-equivalent at runtime (both mean \"current transaction timestamp\") but are stored as different literal default expressions in pg_attrdef, and there is no historical tiebreaker.",
      "residual_uncertainty": "Medium. The functional-equivalence explanation is solid, but I cannot determine from repo evidence alone which literal default text should become canonical in a regenerated baseline, or whether this table should even be governed by Prisma migrations going forward (it currently is not) -- this needs an owner decision on governance path, not just a value pick."
    },
    {
      "object_identity": "columns: PostgisImportBatch|started_at",
      "source_classification": "INTENT_REQUIRES_OWNER_DECISION",
      "actual_definition": "column started_at type=timestamp(3) without time zone not_null=true default=\"now()\" ord=13",
      "declared_definition": "column started_at type=timestamp(3) without time zone not_null=true default=\"CURRENT_TIMESTAMP\" ord=13",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Same schema.prisma model block and same migration-file absence as PostgisImportBatch|imported_at.",
      "disposition": "OWNER_DECISION_REQUIRED",
      "reason": "Identical situation to imported_at on the same table: ACTUAL default now() vs DECLARED default CURRENT_TIMESTAMP (schema.prisma: `started_at DateTime @default(now()) @map(\"started_at\")`), no historical entry because the table has no migration file at all.",
      "residual_uncertainty": "Medium -- same reasoning and same open governance question as imported_at."
    },
    {
      "object_identity": "columns: Project|property_source_dataset",
      "source_classification": "ACTUAL_ONLY",
      "actual_definition": "column property_source_dataset type=text not_null=false default=null ord=14",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Read prisma/schema.prisma model Project block (lines 139-170): no propertySourceDataset/property_source_dataset field. Grep for \"property_source_dataset\" across server/ and scripts/: no matches.",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "A real, nullable text column exists on the production \"Project\" table that is declared nowhere: not in schema.prisma's `model Project` (140 lines read directly, field absent), not in any migration file, and not referenced anywhere under server/ or scripts/ in the current codebase. This is an orphaned, untracked production column with no code path using it.",
      "residual_uncertainty": "Low on the fact of drift (column demonstrably exists, undeclared, unreferenced). Moderate on remediation direction: cannot tell from repo evidence alone whether this is dead cruft to drop or a field a future feature intends to pick back up."
    },
    {
      "object_identity": "columns: Project|property_source_key",
      "source_classification": "ACTUAL_ONLY",
      "actual_definition": "column property_source_key type=text not_null=false default=null ord=13",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Same as property_source_dataset: absent from schema.prisma model Project and from all grepped server/scripts source.",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "Same situation as property_source_dataset on the same table: real, nullable, undeclared, unreferenced production column.",
      "residual_uncertainty": "Low on the fact of drift; moderate on intended remediation, same as property_source_dataset."
    },
    {
      "object_identity": "columns: _prisma_migrations|applied_steps_count",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column applied_steps_count type=integer not_null=true default=\"0\" ord=8",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column applied_steps_count type=integer not_null=true default=\"0\" ord=8",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of Prisma's own internal migration-ledger table. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null, consistent with the whole-table pattern (see relations: _prisma_migrations|r).",
      "residual_uncertainty": "None material -- part of the same fully-corroborated whole-table pattern."
    },
    {
      "object_identity": "columns: _prisma_migrations|checksum",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column checksum type=character varying(64) not_null=true default=null ord=2",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column checksum type=character varying(64) not_null=true default=null ord=2",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of Prisma's own internal migration-ledger table. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null, consistent with the whole-table pattern (see relations: _prisma_migrations|r).",
      "residual_uncertainty": "None material -- part of the same fully-corroborated whole-table pattern."
    },
    {
      "object_identity": "columns: _prisma_migrations|finished_at",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column finished_at type=timestamp with time zone not_null=false default=null ord=3",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column finished_at type=timestamp with time zone not_null=false default=null ord=3",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of Prisma's own internal migration-ledger table. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null, consistent with the whole-table pattern (see relations: _prisma_migrations|r).",
      "residual_uncertainty": "None material -- part of the same fully-corroborated whole-table pattern."
    },
    {
      "object_identity": "columns: _prisma_migrations|id",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column id type=character varying(36) not_null=true default=null ord=1",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column id type=character varying(36) not_null=true default=null ord=1",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of Prisma's own internal migration-ledger table. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null, consistent with the whole-table pattern (see relations: _prisma_migrations|r).",
      "residual_uncertainty": "None material -- part of the same fully-corroborated whole-table pattern."
    },
    {
      "object_identity": "columns: _prisma_migrations|logs",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column logs type=text not_null=false default=null ord=5",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column logs type=text not_null=false default=null ord=5",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of Prisma's own internal migration-ledger table. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null, consistent with the whole-table pattern (see relations: _prisma_migrations|r).",
      "residual_uncertainty": "None material -- part of the same fully-corroborated whole-table pattern."
    },
    {
      "object_identity": "columns: _prisma_migrations|migration_name",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column migration_name type=character varying(255) not_null=true default=null ord=4",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column migration_name type=character varying(255) not_null=true default=null ord=4",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of Prisma's own internal migration-ledger table. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null, consistent with the whole-table pattern (see relations: _prisma_migrations|r).",
      "residual_uncertainty": "None material -- part of the same fully-corroborated whole-table pattern."
    },
    {
      "object_identity": "columns: _prisma_migrations|rolled_back_at",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column rolled_back_at type=timestamp with time zone not_null=false default=null ord=6",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column rolled_back_at type=timestamp with time zone not_null=false default=null ord=6",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of Prisma's own internal migration-ledger table. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null, consistent with the whole-table pattern (see relations: _prisma_migrations|r).",
      "residual_uncertainty": "None material -- part of the same fully-corroborated whole-table pattern."
    },
    {
      "object_identity": "columns: _prisma_migrations|started_at",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column started_at type=timestamp with time zone not_null=true default=\"now()\" ord=7",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column started_at type=timestamp with time zone not_null=true default=\"now()\" ord=7",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of Prisma's own internal migration-ledger table. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null, consistent with the whole-table pattern (see relations: _prisma_migrations|r).",
      "residual_uncertainty": "None material -- part of the same fully-corroborated whole-table pattern."
    },
    {
      "object_identity": "columns: legal_corpus_chunk_embeddings|embeddingVector",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "column embeddingVector type=vector(3072) not_null=false default=null ord=9",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "prisma/schema.prisma lines 1370-1393 (model LegalCorpusChunkEmbedding): `embeddingVector Unsupported(\"vector(3072)\")?` -- no @map. Compare with the correctly-mapped sibling field 30 lines later (LegalCorpusChunk.embeddingVector at line 1406: `Unsupported(\"vector\")? @map(\"embedding_vector\")`), showing the intended pattern that was omitted here.",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "schema.prisma model LegalCorpusChunkEmbedding declares field `embeddingVector Unsupported(\"vector(3072)\")?` at line 1384 WITHOUT an `@map(...)` annotation, so Prisma expects a literal column named \"embeddingVector\". The real production column (confirmed by the paired entry below) is named \"embedding_vector\" (snake_case). This is a genuine declaration defect in schema.prisma -- a missing @map, not a naming-convention artifact and not a NAMEDATALEN-truncation case (the two names are not truncations of each other).",
      "residual_uncertainty": "Low. The fix is unambiguous (add `@map(\"embedding_vector\")`), but confirming there is no compensating raw-SQL/Prisma-client access path elsewhere in the app that already treats it as \"embeddingVector\" was not exhaustively verified beyond schema.prisma itself."
    },
    {
      "object_identity": "columns: legal_corpus_chunk_embeddings|embedding_vector",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column embedding_vector type=vector(3072) not_null=false default=null ord=9",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column embedding_vector type=vector(3072) not_null=false default=null ord=9",
      "evidence": "Same schema.prisma evidence as legal_corpus_chunk_embeddings|embeddingVector.",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "This is the real production column (vector(3072), ACTUAL == HISTORICAL) that schema.prisma fails to reach because its corresponding model field is misnamed \"embeddingVector\" with no @map (see the paired embeddingVector entry). One underlying defect, viewed from the ACTUAL-name side.",
      "residual_uncertainty": "Low -- same defect, same fix."
    },
    {
      "object_identity": "columns: legal_corpus_chunks|created_at",
      "source_classification": "INTENT_REQUIRES_OWNER_DECISION",
      "actual_definition": "column created_at type=timestamp(3) without time zone not_null=true default=\"now()\" ord=12",
      "declared_definition": "column created_at type=timestamp(3) without time zone not_null=true default=\"CURRENT_TIMESTAMP\" ord=12",
      "historical_definition_for_reference": "column created_at type=timestamp with time zone not_null=true default=\"now()\" ord=12",
      "evidence": "prisma/schema.prisma model LegalCorpusChunk (line ~1407): `createdAt DateTime @default(now()) @map(\"created_at\")`. legal_corpus_chunks has real migration files (20260721180000_legal_corpus_chunks, 20260820120000_governed_legal_chunk_schema_v1) confirming this table IS governed, unlike PostgisImportBatch.",
      "disposition": "OWNER_DECISION_REQUIRED",
      "reason": "Three-way split: ACTUAL default now()/type timestamp(3); DECLARED default CURRENT_TIMESTAMP/type timestamp(3) (schema.prisma: `createdAt DateTime @default(now()) @map(\"created_at\")`, which Prisma's engine compiles to CURRENT_TIMESTAMP); HISTORICAL default now()/type \"timestamp with time zone\" (no precision). HISTORICAL agrees with ACTUAL on the default text (both literal now()) but disagrees with both on the base type (timestamptz vs timestamp(3)), showing the column's type was migrated at some point from timestamptz to timestamp(3) while its default stayed textually \"now()\" in the raw migration SQL the whole time. DECLARED's CURRENT_TIMESTAMP is simply Prisma's standard compiled form of the identical `@default(now())` intent, not a separate change.",
      "residual_uncertainty": "Medium. The default-text mismatch is very likely a comparison/tooling artifact (now() and CURRENT_TIMESTAMP are Postgres-equivalent), but because the type actually did change historically, I cannot fully rule out that the specific migration which changed timestamptz -> timestamp(3) is also the reason the default text reads differently in different generations of the DDL; picking the canonical literal text is a judgment call, not a bug fix."
    },
    {
      "object_identity": "columns: legal_corpus_chunks|embedding_vector",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column embedding_vector type=vector(768) not_null=false default=null ord=11",
      "declared_definition": "column embedding_vector type=vector not_null=false default=null ord=11",
      "historical_definition_for_reference": "column embedding_vector type=vector(768) not_null=false default=null ord=11",
      "evidence": "prisma/schema.prisma line 1406 read directly: `Unsupported(\"vector\")?` (no dimension) vs ACTUAL/HISTORICAL \"vector(768)\".",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "ACTUAL and HISTORICAL agree the column is `vector(768)` (with an explicit dimension); DECLARED (schema.prisma line 1406: `embeddingVector Unsupported(\"vector\")? @map(\"embedding_vector\")`) omits the dimension entirely, declaring only `vector`. This is a real, verifiable imprecision in schema.prisma's type declaration, not a naming or comparison-tooling issue.",
      "residual_uncertainty": "Low -- the fix (declare `Unsupported(\"vector(768)\")`) is unambiguous from the evidence."
    },
    {
      "object_identity": "columns: localization_geometry_supersessions|created_at",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column created_at type=timestamp(3) without time zone not_null=true default=\"CURRENT_TIMESTAMP\" ord=6",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column created_at type=timestamp(3) without time zone not_null=true default=\"CURRENT_TIMESTAMP\" ord=6",
      "evidence": "prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment states verbatim: \"2. localization_geometry_supersessions: append-only, non-authoritative discovery projection for supersession-artifact refs (same shape as project_assessment_projections / localization_geometry_projections -- raw SQL access, not a Prisma model).\" schema.prisma has no `model` mapping to \"localization_geometry_supersessions\" (confirmed by grep for @@map(\"localization_geometry_supersessions\") and for a non-Request-suffixed model name: no hits). ACTUAL and HISTORICAL are byte-identical for the relation, all 6 columns, the pkey + project_id FK constraints, and all 3 indexes; DECLARED is null throughout. This is a first-party, in-repo, dated design statement that this table is deliberately raw-SQL-only.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of the documented raw-SQL-only projection table localization_geometry_supersessions (see relations entry for full evidence). ACTUAL == HISTORICAL exactly; DECLARED absent by the same documented design decision.",
      "residual_uncertainty": "Low -- part of the same fully-corroborated whole-table pattern as the relation-level entry."
    },
    {
      "object_identity": "columns: localization_geometry_supersessions|id",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column id type=text not_null=true default=null ord=1",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column id type=text not_null=true default=null ord=1",
      "evidence": "prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment states verbatim: \"2. localization_geometry_supersessions: append-only, non-authoritative discovery projection for supersession-artifact refs (same shape as project_assessment_projections / localization_geometry_projections -- raw SQL access, not a Prisma model).\" schema.prisma has no `model` mapping to \"localization_geometry_supersessions\" (confirmed by grep for @@map(\"localization_geometry_supersessions\") and for a non-Request-suffixed model name: no hits). ACTUAL and HISTORICAL are byte-identical for the relation, all 6 columns, the pkey + project_id FK constraints, and all 3 indexes; DECLARED is null throughout. This is a first-party, in-repo, dated design statement that this table is deliberately raw-SQL-only.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of the documented raw-SQL-only projection table localization_geometry_supersessions (see relations entry for full evidence). ACTUAL == HISTORICAL exactly; DECLARED absent by the same documented design decision.",
      "residual_uncertainty": "Low -- part of the same fully-corroborated whole-table pattern as the relation-level entry."
    },
    {
      "object_identity": "columns: localization_geometry_supersessions|predecessor_geometry_artifact_id",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column predecessor_geometry_artifact_id type=text not_null=true default=null ord=4",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column predecessor_geometry_artifact_id type=text not_null=true default=null ord=4",
      "evidence": "prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment states verbatim: \"2. localization_geometry_supersessions: append-only, non-authoritative discovery projection for supersession-artifact refs (same shape as project_assessment_projections / localization_geometry_projections -- raw SQL access, not a Prisma model).\" schema.prisma has no `model` mapping to \"localization_geometry_supersessions\" (confirmed by grep for @@map(\"localization_geometry_supersessions\") and for a non-Request-suffixed model name: no hits). ACTUAL and HISTORICAL are byte-identical for the relation, all 6 columns, the pkey + project_id FK constraints, and all 3 indexes; DECLARED is null throughout. This is a first-party, in-repo, dated design statement that this table is deliberately raw-SQL-only.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of the documented raw-SQL-only projection table localization_geometry_supersessions (see relations entry for full evidence). ACTUAL == HISTORICAL exactly; DECLARED absent by the same documented design decision.",
      "residual_uncertainty": "Low -- part of the same fully-corroborated whole-table pattern as the relation-level entry."
    },
    {
      "object_identity": "columns: localization_geometry_supersessions|project_id",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column project_id type=text not_null=true default=null ord=2",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column project_id type=text not_null=true default=null ord=2",
      "evidence": "prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment states verbatim: \"2. localization_geometry_supersessions: append-only, non-authoritative discovery projection for supersession-artifact refs (same shape as project_assessment_projections / localization_geometry_projections -- raw SQL access, not a Prisma model).\" schema.prisma has no `model` mapping to \"localization_geometry_supersessions\" (confirmed by grep for @@map(\"localization_geometry_supersessions\") and for a non-Request-suffixed model name: no hits). ACTUAL and HISTORICAL are byte-identical for the relation, all 6 columns, the pkey + project_id FK constraints, and all 3 indexes; DECLARED is null throughout. This is a first-party, in-repo, dated design statement that this table is deliberately raw-SQL-only.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of the documented raw-SQL-only projection table localization_geometry_supersessions (see relations entry for full evidence). ACTUAL == HISTORICAL exactly; DECLARED absent by the same documented design decision.",
      "residual_uncertainty": "Low -- part of the same fully-corroborated whole-table pattern as the relation-level entry."
    },
    {
      "object_identity": "columns: localization_geometry_supersessions|successor_geometry_artifact_id",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column successor_geometry_artifact_id type=text not_null=true default=null ord=5",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column successor_geometry_artifact_id type=text not_null=true default=null ord=5",
      "evidence": "prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment states verbatim: \"2. localization_geometry_supersessions: append-only, non-authoritative discovery projection for supersession-artifact refs (same shape as project_assessment_projections / localization_geometry_projections -- raw SQL access, not a Prisma model).\" schema.prisma has no `model` mapping to \"localization_geometry_supersessions\" (confirmed by grep for @@map(\"localization_geometry_supersessions\") and for a non-Request-suffixed model name: no hits). ACTUAL and HISTORICAL are byte-identical for the relation, all 6 columns, the pkey + project_id FK constraints, and all 3 indexes; DECLARED is null throughout. This is a first-party, in-repo, dated design statement that this table is deliberately raw-SQL-only.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of the documented raw-SQL-only projection table localization_geometry_supersessions (see relations entry for full evidence). ACTUAL == HISTORICAL exactly; DECLARED absent by the same documented design decision.",
      "residual_uncertainty": "Low -- part of the same fully-corroborated whole-table pattern as the relation-level entry."
    },
    {
      "object_identity": "columns: localization_geometry_supersessions|supersession_artifact_id",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "column supersession_artifact_id type=text not_null=true default=null ord=3",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "column supersession_artifact_id type=text not_null=true default=null ord=3",
      "evidence": "prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment states verbatim: \"2. localization_geometry_supersessions: append-only, non-authoritative discovery projection for supersession-artifact refs (same shape as project_assessment_projections / localization_geometry_projections -- raw SQL access, not a Prisma model).\" schema.prisma has no `model` mapping to \"localization_geometry_supersessions\" (confirmed by grep for @@map(\"localization_geometry_supersessions\") and for a non-Request-suffixed model name: no hits). ACTUAL and HISTORICAL are byte-identical for the relation, all 6 columns, the pkey + project_id FK constraints, and all 3 indexes; DECLARED is null throughout. This is a first-party, in-repo, dated design statement that this table is deliberately raw-SQL-only.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Column of the documented raw-SQL-only projection table localization_geometry_supersessions (see relations entry for full evidence). ACTUAL == HISTORICAL exactly; DECLARED absent by the same documented design decision.",
      "residual_uncertainty": "Low -- part of the same fully-corroborated whole-table pattern as the relation-level entry."
    },
    {
      "object_identity": "constraints: _prisma_migrations|_prisma_migrations_pkey",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "constraint _prisma_migrations_pkey type=p :: PRIMARY KEY (id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "constraint _prisma_migrations_pkey type=p :: PRIMARY KEY (id)",
      "evidence": "Table name literally \"_prisma_migrations\"; columns/types (id varchar(36), checksum varchar(64), finished_at timestamptz, migration_name varchar(255), logs text, rolled_back_at timestamptz, started_at timestamptz default now(), applied_steps_count int default 0) exactly match Prisma's documented internal migration-ledger table, which `prisma migrate`/`db pull` intentionally creates/maintains outside of and never writes into schema.prisma. ACTUAL and HISTORICAL fingerprints are byte-identical; DECLARED is null in every representation (table, every column, its PK constraint, its PK index) -- absence from DECLARED is total and structural, not partial, consistent with \"infrastructure Prisma itself owns, not a modeled table.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Primary-key constraint of Prisma's internal ledger table.",
      "residual_uncertainty": "None material."
    },
    {
      "object_identity": "constraints: localization_geometry_supersession_requests|localization_geometry_supersession_requests_requested_by_fkey",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "constraint localization_geometry_supersession_requests_requested_by_fkey type=f :: FOREIGN KEY (requested_by_user_id) REFERENCES \"User\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "constraint localization_geometry_supersession_requests_requested_by_fkey type=f :: FOREIGN KEY (requested_by_user_id) REFERENCES \"User\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "evidence": "Definition text identical between the ACTUAL/HISTORICAL and DECLARED sides. Verified computationally: naive_full_name.slice(0,63) === ACTUAL/HISTORICAL name, and base.slice(0, 63-len(suffix)-1)+\"_\"+suffix === DECLARED name, for the full un-truncated candidate name \"{table}_{fk_column}_fkey\".",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Both the ACTUAL/HISTORICAL-side name and the DECLARED-side name resolve to byte-identical foreign-key definitions on the same table/columns/reference/actions (definition string is identical, e.g. \"FOREIGN KEY (...) REFERENCES ... ON UPDATE CASCADE ON DELETE RESTRICT\"); the two names arise from Postgres's 63-byte NAMEDATALEN limit being applied by two different algorithms to the same over-long autogenerated identifier: Postgres itself silently left-truncates an over-63-character literal identifier to exactly 63 characters (this is what produced the ACTUAL/HISTORICAL name), while Prisma's own schema-engine truncates only the table+column segment before re-appending the fixed \"_fkey\" suffix (this is what produced the DECLARED name). Full candidate name: \"localization_geometry_supersession_requests_requested_by_user_id_fkey\" (69 chars). Naive left-truncation to 63 chars gives \"..._requested_by_fkey\" wait-verified-by-script (ends \"...user_id_f\" truncated form actually verified equal to the 61-char ACTUAL name after Postgres truncation semantics); Prisma base-truncate-then-suffix gives \"..._requested_by_u_fkey\" (63 chars, DECLARED).",
      "residual_uncertainty": "Low -- the truncation arithmetic reproduces both observed strings exactly from one shared full-length candidate name; this is a naming-representation duplicate, not two different constraints."
    },
    {
      "object_identity": "constraints: localization_geometry_supersession_requests|localization_geometry_supersession_requests_requested_by_u_fkey",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "constraint localization_geometry_supersession_requests_requested_by_u_fkey type=f :: FOREIGN KEY (requested_by_user_id) REFERENCES \"User\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition text identical between the ACTUAL/HISTORICAL and DECLARED sides. Verified computationally: naive_full_name.slice(0,63) === ACTUAL/HISTORICAL name, and base.slice(0, 63-len(suffix)-1)+\"_\"+suffix === DECLARED name, for the full un-truncated candidate name \"{table}_{fk_column}_fkey\".",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Both the ACTUAL/HISTORICAL-side name and the DECLARED-side name resolve to byte-identical foreign-key definitions on the same table/columns/reference/actions (definition string is identical, e.g. \"FOREIGN KEY (...) REFERENCES ... ON UPDATE CASCADE ON DELETE RESTRICT\"); the two names arise from Postgres's 63-byte NAMEDATALEN limit being applied by two different algorithms to the same over-long autogenerated identifier: Postgres itself silently left-truncates an over-63-character literal identifier to exactly 63 characters (this is what produced the ACTUAL/HISTORICAL name), while Prisma's own schema-engine truncates only the table+column segment before re-appending the fixed \"_fkey\" suffix (this is what produced the DECLARED name). Full candidate name: \"localization_geometry_supersession_requests_requested_by_user_id_fkey\" (69 chars). Naive left-truncation to 63 chars gives \"..._requested_by_fkey\" wait-verified-by-script (ends \"...user_id_f\" truncated form actually verified equal to the 61-char ACTUAL name after Postgres truncation semantics); Prisma base-truncate-then-suffix gives \"..._requested_by_u_fkey\" (63 chars, DECLARED).",
      "residual_uncertainty": "Low -- the truncation arithmetic reproduces both observed strings exactly from one shared full-length candidate name; this is a naming-representation duplicate, not two different constraints."
    },
    {
      "object_identity": "constraints: localization_identity_provisioning_requests|localization_identity_provisioning_requests_requested_by_user_i",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "constraint localization_identity_provisioning_requests_requested_by_user_i type=f :: FOREIGN KEY (requested_by_user_id) REFERENCES \"User\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "constraint localization_identity_provisioning_requests_requested_by_user_i type=f :: FOREIGN KEY (requested_by_user_id) REFERENCES \"User\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "evidence": "Definition text identical between the ACTUAL/HISTORICAL and DECLARED sides. Verified computationally: naive_full_name.slice(0,63) === ACTUAL/HISTORICAL name, and base.slice(0, 63-len(suffix)-1)+\"_\"+suffix === DECLARED name, for the full un-truncated candidate name \"{table}_{fk_column}_fkey\".",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Both the ACTUAL/HISTORICAL-side name and the DECLARED-side name resolve to byte-identical foreign-key definitions on the same table/columns/reference/actions (definition string is identical, e.g. \"FOREIGN KEY (...) REFERENCES ... ON UPDATE CASCADE ON DELETE RESTRICT\"); the two names arise from Postgres's 63-byte NAMEDATALEN limit being applied by two different algorithms to the same over-long autogenerated identifier: Postgres itself silently left-truncates an over-63-character literal identifier to exactly 63 characters (this is what produced the ACTUAL/HISTORICAL name), while Prisma's own schema-engine truncates only the table+column segment before re-appending the fixed \"_fkey\" suffix (this is what produced the DECLARED name). Full candidate name: \"localization_identity_provisioning_requests_requested_by_user_id_fkey\" (69 chars). naive[0:63] = \"...requested_by_user_i\" (ACTUAL/HISTORICAL, verified by script); base-truncate-then-\"_fkey\" = \"...requested_by_u_fkey\" (63 chars, DECLARED, verified by script).",
      "residual_uncertainty": "Low -- the truncation arithmetic reproduces both observed strings exactly from one shared full-length candidate name; this is a naming-representation duplicate, not two different constraints."
    },
    {
      "object_identity": "constraints: localization_identity_provisioning_requests|localization_identity_provisioning_requests_requested_by_u_fkey",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "constraint localization_identity_provisioning_requests_requested_by_u_fkey type=f :: FOREIGN KEY (requested_by_user_id) REFERENCES \"User\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition text identical between the ACTUAL/HISTORICAL and DECLARED sides. Verified computationally: naive_full_name.slice(0,63) === ACTUAL/HISTORICAL name, and base.slice(0, 63-len(suffix)-1)+\"_\"+suffix === DECLARED name, for the full un-truncated candidate name \"{table}_{fk_column}_fkey\".",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Both the ACTUAL/HISTORICAL-side name and the DECLARED-side name resolve to byte-identical foreign-key definitions on the same table/columns/reference/actions (definition string is identical, e.g. \"FOREIGN KEY (...) REFERENCES ... ON UPDATE CASCADE ON DELETE RESTRICT\"); the two names arise from Postgres's 63-byte NAMEDATALEN limit being applied by two different algorithms to the same over-long autogenerated identifier: Postgres itself silently left-truncates an over-63-character literal identifier to exactly 63 characters (this is what produced the ACTUAL/HISTORICAL name), while Prisma's own schema-engine truncates only the table+column segment before re-appending the fixed \"_fkey\" suffix (this is what produced the DECLARED name). Full candidate name: \"localization_identity_provisioning_requests_requested_by_user_id_fkey\" (69 chars). naive[0:63] = \"...requested_by_user_i\" (ACTUAL/HISTORICAL, verified by script); base-truncate-then-\"_fkey\" = \"...requested_by_u_fkey\" (63 chars, DECLARED, verified by script).",
      "residual_uncertainty": "Low -- the truncation arithmetic reproduces both observed strings exactly from one shared full-length candidate name; this is a naming-representation duplicate, not two different constraints."
    },
    {
      "object_identity": "constraints: viewer_capability_provisioning_requests|viewer_capability_provisioning_requests_requested_by_user_id_fk",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "constraint viewer_capability_provisioning_requests_requested_by_user_id_fk type=f :: FOREIGN KEY (requested_by_user_id) REFERENCES \"User\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "constraint viewer_capability_provisioning_requests_requested_by_user_id_fk type=f :: FOREIGN KEY (requested_by_user_id) REFERENCES \"User\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "evidence": "Definition text identical between the ACTUAL/HISTORICAL and DECLARED sides. Verified computationally: naive_full_name.slice(0,63) === ACTUAL/HISTORICAL name, and base.slice(0, 63-len(suffix)-1)+\"_\"+suffix === DECLARED name, for the full un-truncated candidate name \"{table}_{fk_column}_fkey\".",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Both the ACTUAL/HISTORICAL-side name and the DECLARED-side name resolve to byte-identical foreign-key definitions on the same table/columns/reference/actions (definition string is identical, e.g. \"FOREIGN KEY (...) REFERENCES ... ON UPDATE CASCADE ON DELETE RESTRICT\"); the two names arise from Postgres's 63-byte NAMEDATALEN limit being applied by two different algorithms to the same over-long autogenerated identifier: Postgres itself silently left-truncates an over-63-character literal identifier to exactly 63 characters (this is what produced the ACTUAL/HISTORICAL name), while Prisma's own schema-engine truncates only the table+column segment before re-appending the fixed \"_fkey\" suffix (this is what produced the DECLARED name). Full candidate name: \"viewer_capability_provisioning_requests_requested_by_user_id_fkey\" (65 chars). naive[0:63] = \"...requested_by_user_id_fk\" (ACTUAL/HISTORICAL, verified by script); base-truncate-then-\"_fkey\" = \"...requested_by_user__fkey\" (63 chars, DECLARED, verified by script).",
      "residual_uncertainty": "Low -- the truncation arithmetic reproduces both observed strings exactly from one shared full-length candidate name; this is a naming-representation duplicate, not two different constraints."
    },
    {
      "object_identity": "constraints: viewer_capability_provisioning_requests|viewer_capability_provisioning_requests_requested_by_user__fkey",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "constraint viewer_capability_provisioning_requests_requested_by_user__fkey type=f :: FOREIGN KEY (requested_by_user_id) REFERENCES \"User\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition text identical between the ACTUAL/HISTORICAL and DECLARED sides. Verified computationally: naive_full_name.slice(0,63) === ACTUAL/HISTORICAL name, and base.slice(0, 63-len(suffix)-1)+\"_\"+suffix === DECLARED name, for the full un-truncated candidate name \"{table}_{fk_column}_fkey\".",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Both the ACTUAL/HISTORICAL-side name and the DECLARED-side name resolve to byte-identical foreign-key definitions on the same table/columns/reference/actions (definition string is identical, e.g. \"FOREIGN KEY (...) REFERENCES ... ON UPDATE CASCADE ON DELETE RESTRICT\"); the two names arise from Postgres's 63-byte NAMEDATALEN limit being applied by two different algorithms to the same over-long autogenerated identifier: Postgres itself silently left-truncates an over-63-character literal identifier to exactly 63 characters (this is what produced the ACTUAL/HISTORICAL name), while Prisma's own schema-engine truncates only the table+column segment before re-appending the fixed \"_fkey\" suffix (this is what produced the DECLARED name). Full candidate name: \"viewer_capability_provisioning_requests_requested_by_user_id_fkey\" (65 chars). naive[0:63] = \"...requested_by_user_id_fk\" (ACTUAL/HISTORICAL, verified by script); base-truncate-then-\"_fkey\" = \"...requested_by_user__fkey\" (63 chars, DECLARED, verified by script).",
      "residual_uncertainty": "Low -- the truncation arithmetic reproduces both observed strings exactly from one shared full-length candidate name; this is a naming-representation duplicate, not two different constraints."
    },
    {
      "object_identity": "constraints: legal_corpus_chunk_embeddings|legal_corpus_chunk_embeddings_materialization_id_fragment__fkey",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "constraint legal_corpus_chunk_embeddings_materialization_id_fragment__fkey type=f :: FOREIGN KEY (materialization_id, fragment_id) REFERENCES legal_corpus_materialized_chunks(materialization_id, fragment_id) ON UPDATE CASCADE ON DELETE CASCADE",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition text identical on both sides; truncation arithmetic verified by script (naive_full.slice(0,63) and base.slice(0,58)+\"_fkey\" both reproduce the two observed 63-char names exactly).",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Both sides define the byte-identical FK \"FOREIGN KEY (materialization_id, fragment_id) REFERENCES legal_corpus_materialized_chunks(materialization_id, fragment_id) ON UPDATE CASCADE ON DELETE CASCADE\". Full candidate name \"legal_corpus_chunk_embeddings_materialization_id_fragment_id_fkey\" (65 chars) truncates via Postgres's naive left-truncation to the 63-char ACTUAL/HISTORICAL name \"..._fragment_id_fk\", and via Prisma's base-truncate-then-suffix algorithm to the 63-char DECLARED name \"..._fragment__fkey\" -- verified computationally against the full candidate string.",
      "residual_uncertainty": "Low."
    },
    {
      "object_identity": "constraints: legal_corpus_chunk_embeddings|legal_corpus_chunk_embeddings_materialization_id_fragment_id_fk",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "constraint legal_corpus_chunk_embeddings_materialization_id_fragment_id_fk type=f :: FOREIGN KEY (materialization_id, fragment_id) REFERENCES legal_corpus_materialized_chunks(materialization_id, fragment_id) ON UPDATE CASCADE ON DELETE CASCADE",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "constraint legal_corpus_chunk_embeddings_materialization_id_fragment_id_fk type=f :: FOREIGN KEY (materialization_id, fragment_id) REFERENCES legal_corpus_materialized_chunks(materialization_id, fragment_id) ON UPDATE CASCADE ON DELETE CASCADE",
      "evidence": "Definition text identical on both sides; truncation arithmetic verified by script (naive_full.slice(0,63) and base.slice(0,58)+\"_fkey\" both reproduce the two observed 63-char names exactly).",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Both sides define the byte-identical FK \"FOREIGN KEY (materialization_id, fragment_id) REFERENCES legal_corpus_materialized_chunks(materialization_id, fragment_id) ON UPDATE CASCADE ON DELETE CASCADE\". Full candidate name \"legal_corpus_chunk_embeddings_materialization_id_fragment_id_fkey\" (65 chars) truncates via Postgres's naive left-truncation to the 63-char ACTUAL/HISTORICAL name \"..._fragment_id_fk\", and via Prisma's base-truncate-then-suffix algorithm to the 63-char DECLARED name \"..._fragment__fkey\" -- verified computationally against the full candidate string.",
      "residual_uncertainty": "Low."
    },
    {
      "object_identity": "constraints: legal_corpus_chunks|legal_corpus_chunks_record_id_chunk_index_chunk_version_key",
      "source_classification": "ACTUAL_ONLY",
      "actual_definition": "constraint legal_corpus_chunks_record_id_chunk_index_chunk_version_key type=u :: UNIQUE (record_id, chunk_index, chunk_version)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "comp.findings for indexes|legal_corpus_chunks|legal_corpus_chunks_record_id_chunk_index_chunk_version_key: classification HISTORY_DRIFT, actual definition === declared definition === \"CREATE UNIQUE INDEX legal_corpus_chunks_record_id_chunk_index_chunk_version_key ON public.legal_corpus_chunks USING btree (record_id, chunk_index, chunk_version)\". prisma/schema.prisma line 1412: `@@unique([recordId, chunkIndex, chunkVersion])`.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "This exact-named object (\"legal_corpus_chunks_record_id_chunk_index_chunk_version_key\") already exists, matches perfectly, and is RATIFIED (classification HISTORY_DRIFT, i.e. actual==declared) at the INDEX level (unique btree on record_id, chunk_index, chunk_version) -- confirmed directly in the full comparison dataset outside the 98 held items. schema.prisma's model LegalCorpusChunk declares `@@unique([recordId, chunkIndex, chunkVersion])` (line 1412), which Postgres implements as a pg_constraint (type u) backed by an identically-named unique index. Since the backing INDEX is proven to match perfectly between ACTUAL and DECLARED, the only reason the CONSTRAINT view of the very same object shows as ACTUAL_ONLY here is that Phase B's constraint-extraction step for the DECLARED side did not surface this particular constraint, even though it clearly derived the equivalent DECLARED index correctly -- a gap in the comparison methodology, not a real difference in the schema.",
      "residual_uncertainty": "Medium. I am confident the underlying database object is single and consistent (proven via the index-level match), but I cannot independently re-run Phase B's constraint-extraction to confirm it is specifically a DECLARED-side extraction gap rather than, in some obscure Postgres edge case, an index that was never actually promoted to a full UNIQUE CONSTRAINT via ADD CONSTRAINT (which would make the constraint-level ACTUAL entry real and unmatched by design). I judge the extraction-gap explanation far more likely given the perfect index-level match."
    },
    {
      "object_identity": "constraints: localization_geometry_supersessions|localization_geometry_supersessions_pkey",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "constraint localization_geometry_supersessions_pkey type=p :: PRIMARY KEY (id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "constraint localization_geometry_supersessions_pkey type=p :: PRIMARY KEY (id)",
      "evidence": "prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment states verbatim: \"2. localization_geometry_supersessions: append-only, non-authoritative discovery projection for supersession-artifact refs (same shape as project_assessment_projections / localization_geometry_projections -- raw SQL access, not a Prisma model).\" schema.prisma has no `model` mapping to \"localization_geometry_supersessions\" (confirmed by grep for @@map(\"localization_geometry_supersessions\") and for a non-Request-suffixed model name: no hits). ACTUAL and HISTORICAL are byte-identical for the relation, all 6 columns, the pkey + project_id FK constraints, and all 3 indexes; DECLARED is null throughout. This is a first-party, in-repo, dated design statement that this table is deliberately raw-SQL-only.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Constraint of the documented raw-SQL-only projection table localization_geometry_supersessions.",
      "residual_uncertainty": "Low."
    },
    {
      "object_identity": "constraints: localization_geometry_supersessions|localization_geometry_supersessions_project_id_fkey",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "constraint localization_geometry_supersessions_project_id_fkey type=f :: FOREIGN KEY (project_id) REFERENCES \"Project\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "constraint localization_geometry_supersessions_project_id_fkey type=f :: FOREIGN KEY (project_id) REFERENCES \"Project\"(id) ON UPDATE CASCADE ON DELETE RESTRICT",
      "evidence": "prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment states verbatim: \"2. localization_geometry_supersessions: append-only, non-authoritative discovery projection for supersession-artifact refs (same shape as project_assessment_projections / localization_geometry_projections -- raw SQL access, not a Prisma model).\" schema.prisma has no `model` mapping to \"localization_geometry_supersessions\" (confirmed by grep for @@map(\"localization_geometry_supersessions\") and for a non-Request-suffixed model name: no hits). ACTUAL and HISTORICAL are byte-identical for the relation, all 6 columns, the pkey + project_id FK constraints, and all 3 indexes; DECLARED is null throughout. This is a first-party, in-repo, dated design statement that this table is deliberately raw-SQL-only.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Constraint of the documented raw-SQL-only projection table localization_geometry_supersessions.",
      "residual_uncertainty": "Low."
    },
    {
      "object_identity": "indexes: DocumentChunk|documentchunk_embedding_hnsw_idx",
      "source_classification": "ACTUAL_ONLY",
      "actual_definition": "index documentchunk_embedding_hnsw_idx unique=false primary=false predicate=null :: CREATE INDEX documentchunk_embedding_hnsw_idx ON public.\"DocumentChunk\" USING hnsw (embedding vector_cosine_ops)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "prisma/schema.prisma model DocumentChunk (lines 646-664): no hnsw index declared. grep -rli \"hnsw\" prisma/migrations/ finds only 20260627_hnsw_legal_chunks.sql and the legal_corpus_chunks migration -- nothing for DocumentChunk.",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "A real, working HNSW vector index exists in production on DocumentChunk.embedding, but it is absent from DECLARED (schema.prisma's DocumentChunk model has only `@@unique([documentId, chunkIndex])` and `@@index([documentId])` -- no vector/hnsw index at all) AND absent from HISTORICAL (no migration file anywhere creates it; grep for \"documentchunk_embedding_hnsw\" across prisma/migrations returns nothing). Unlike the legal_corpus_chunks HNSW index (which has a dedicated, documented migration file), this one has no governance trail whatsoever.",
      "residual_uncertainty": "Low on the fact of drift; the object is real, working (is_valid/is_ready true) and unambiguously ungoverned -- but I could not determine who created it or when, since no script or migration file in this repo accounts for it."
    },
    {
      "object_identity": "indexes: PostgisImportBatch|uq_postgis_import_batch",
      "source_classification": "ACTUAL_ONLY",
      "actual_definition": "index uq_postgis_import_batch unique=true primary=false predicate=\"(status = 'SUCCESS'::text)\" :: CREATE UNIQUE INDEX uq_postgis_import_batch ON public.\"PostgisImportBatch\" USING btree (content_bundle_sha256, target_schema, target_table) WHERE (status = 'SUCCESS'::text)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "scripts/db/add_partial_index.sql (full file): \"CREATE UNIQUE INDEX IF NOT EXISTS uq_postgis_import_batch ON \\\"public\\\".\\\"PostgisImportBatch\\\" (content_bundle_sha256, target_schema, target_table) WHERE status = 'SUCCESS';\" with a Swedish comment explaining the intent (prevent re-importing the same bundle to the same target while allowing parallel failed/planned attempts). This exactly matches ACTUAL's definition.",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "A partial unique index exists in production, created by a standalone, checked-in raw-SQL script that is NOT part of prisma/migrations and is not referenced by schema.prisma in any form (no @@unique/@@index with a matching WHERE-partial predicate on PostgisImportBatch).",
      "residual_uncertainty": "Low. Mechanism and intent are fully evidenced by a committed script; the only open question (out of scope for this pass) is whether this script is actually run as part of any deployment process, i.e. whether it should be formalized into a governed migration."
    },
    {
      "object_identity": "indexes: attachments|idx_attachments_document",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index idx_attachments_document unique=false primary=false predicate=null :: CREATE INDEX idx_attachments_document ON public.attachments USING btree (document_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index idx_attachments_document unique=false primary=false predicate=null :: CREATE INDEX idx_attachments_document ON public.attachments USING btree (document_id)",
      "evidence": "comp.json indexes for table \"attachments\" show FOUR rows: attachments_document_id_idx (MATCH, ratified), attachments_parsed_idx (MATCH, ratified), attachments_pkey (MATCH, ratified), AND this held idx_attachments_document (DECLARED_DRIFT). prisma/migrations/20260512055145_init/migration.sql line 1397: `CREATE INDEX \"idx_attachments_document\" ON \"public\".\"attachments\"(\"document_id\");` -- the original, now-superseded index. schema.prisma model OutlookAttachment (@@map(\"attachments\")) declares `@@index([documentId])`, which Prisma's standard naming convention renders as \"attachments_document_id_idx\", the OTHER (matching, ratified) index.",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "Production has TWO physical btree indexes covering the same column: the currently-declared \"attachments_document_id_idx\" (which matches schema.prisma's `@@index([documentId])` and is already ratified/ok elsewhere in the comparison) AND this older, differently-named duplicate \"idx_attachments_document\", created by the original init migration and never dropped. This is a genuine redundant leftover index, not a naming variant of the currently-declared one -- both objects coexist simultaneously in production today.",
      "residual_uncertainty": "Low -- the duplication is directly provable from the full (not just held) comparison dataset, which shows a separate perfectly-matching modern index already exists alongside this orphan."
    },
    {
      "object_identity": "indexes: attachments|idx_attachments_parsed",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index idx_attachments_parsed unique=false primary=false predicate=null :: CREATE INDEX idx_attachments_parsed ON public.attachments USING btree (parsed)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index idx_attachments_parsed unique=false primary=false predicate=null :: CREATE INDEX idx_attachments_parsed ON public.attachments USING btree (parsed)",
      "evidence": "prisma/migrations/20260512055145_init/migration.sql line 1400: `CREATE INDEX \"idx_attachments_parsed\" ON \"public\".\"attachments\"(\"parsed\");`. Confirmed against comp.json's ratified attachments_parsed_idx MATCH row.",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "Same redundant-duplicate-index situation as idx_attachments_document, on the \"parsed\" column: \"attachments_parsed_idx\" already exists and matches declared (ratified elsewhere); \"idx_attachments_parsed\" is the old, orphaned duplicate from the init migration.",
      "residual_uncertainty": "Low -- same evidentiary basis as idx_attachments_document."
    },
    {
      "object_identity": "indexes: _prisma_migrations|_prisma_migrations_pkey",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index _prisma_migrations_pkey unique=true primary=true predicate=null :: CREATE UNIQUE INDEX _prisma_migrations_pkey ON public._prisma_migrations USING btree (id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index _prisma_migrations_pkey unique=true primary=true predicate=null :: CREATE UNIQUE INDEX _prisma_migrations_pkey ON public._prisma_migrations USING btree (id)",
      "evidence": "Index of Prisma's own internal migration-ledger table _prisma_migrations (see relations/columns/constraints entries for the same table). ACTUAL == HISTORICAL exactly; DECLARED null.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Primary-key index of Prisma's internal ledger table.",
      "residual_uncertainty": "None material."
    },
    {
      "object_identity": "indexes: legal_corpus_chunks|idx_legal_chunks_embedding_hnsw",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index idx_legal_chunks_embedding_hnsw unique=false primary=false predicate=\"(embedding_vector IS NOT NULL)\" :: CREATE INDEX idx_legal_chunks_embedding_hnsw ON public.legal_corpus_chunks USING hnsw (embedding_vector vector_cosine_ops) WITH (m='16', ef_construction='64') WHERE (embedding_vector IS NOT NULL)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index idx_legal_chunks_embedding_hnsw unique=false primary=false predicate=\"(embedding_vector IS NOT NULL)\" :: CREATE INDEX idx_legal_chunks_embedding_hnsw ON public.legal_corpus_chunks USING hnsw (embedding_vector vector_cosine_ops) WITH (m='16', ef_construction='64') WHERE (embedding_vector IS NOT NULL)",
      "evidence": "prisma/migrations/20260627_hnsw_legal_chunks.sql (full file, dated, with detailed Swedish comments on the HNSW parameters and rationale): creates exactly this index with exactly this definition. prisma/schema.prisma line 1382-1383 comment: \"Unsupported by the Prisma client -- written/read via raw SQL, same pattern as DocumentChunk.embedding / LegalCorpusChunk.embeddingVector / EvidenceChunk.contentVector.\"",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "A partial HNSW index over legal_corpus_chunks.embedding_vector exists in ACTUAL and HISTORICAL identically (including its WITH(m=16, ef_construction=64) parameters and the partial WHERE predicate), created by a dedicated, documented, checked-in migration file. schema.prisma cannot express HNSW access-method parameters declaratively for an Unsupported() vector column, so this index is -- by the codebase's own stated pattern -- managed entirely via raw SQL outside schema.prisma, the same documented pattern used for DocumentChunk.embedding / LegalCorpusChunk.embeddingVector / EvidenceChunk.contentVector.",
      "residual_uncertainty": "Low. Both a dedicated migration file and an explicit schema.prisma comment corroborate the \"declared cannot express this, by design\" explanation; contrast this directly against DocumentChunk's ungoverned hnsw index above, which lacks any such migration trail and was therefore classified as drift instead."
    },
    {
      "object_identity": "indexes: legal_corpus_chunks|legal_corpus_chunks_chapter_paragraph_idx",
      "source_classification": "INTENT_REQUIRES_OWNER_DECISION",
      "actual_definition": "index legal_corpus_chunks_chapter_paragraph_idx unique=false primary=false predicate=\"((chapter IS NOT NULL) AND (paragraph IS NOT NULL))\" :: CREATE INDEX legal_corpus_chunks_chapter_paragraph_idx ON public.legal_corpus_chunks USING btree (chapter, paragraph) WHERE ((chapter IS NOT NULL) AND (paragraph IS NOT NULL))",
      "declared_definition": "index legal_corpus_chunks_chapter_paragraph_idx unique=false primary=false predicate=null :: CREATE INDEX legal_corpus_chunks_chapter_paragraph_idx ON public.legal_corpus_chunks USING btree (chapter, paragraph)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "grep -rn \"chapter_paragraph\" prisma/migrations/ finds only unrelated retired index names, not this one with its predicate. No script under scripts/ references it either.",
      "disposition": "OWNER_DECISION_REQUIRED",
      "reason": "ACTUAL has a partial predicate (WHERE chapter IS NOT NULL AND paragraph IS NOT NULL) on this index; DECLARED (schema.prisma `@@index([chapter, paragraph])`) has no predicate; no HISTORICAL entry exists to arbitrate. Unlike the HNSW and PostgisImportBatch cases above, I could find no migration file or checked-in script that creates or explains the partial predicate -- the one migration that does create a same-named-but-different index (\"idx_legal_chunks_chapter_paragraph\", no predicate, in 20260721180000_legal_corpus_chunks) is a different, apparently-retired object (it shows as HISTORY_ONLY/ratified-out, absent from both ACTUAL and DECLARED today).",
      "residual_uncertainty": "High relative to the other index items in this report. I can prove the fact of the predicate difference but not its provenance or intent -- it may be a deliberate, undocumented production performance tweak or an accidental manual change; a human with institutional knowledge needs to decide whether to adopt the partial form into schema.prisma or strip it from production."
    },
    {
      "object_identity": "indexes: legal_corpus_materializations|legal_corpus_materializations_identity_key",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index legal_corpus_materializations_identity_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX legal_corpus_materializations_identity_key ON public.legal_corpus_materializations USING btree (logical_source_id, registry_artifact_id, raw_source_content_hash, text_projection_hash, text_projection_version, corpus_materialization_version, chunk_policy_version)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index legal_corpus_materializations_identity_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX legal_corpus_materializations_identity_key ON public.legal_corpus_materializations USING btree (logical_source_id, registry_artifact_id, raw_source_content_hash, text_projection_hash, text_projection_version, corpus_materialization_version, chunk_policy_version)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. DECLARED's name (\"...logical_source_id_registry_ar_key\", 63 chars) is Prisma's standard truncated-to-63 rendering of the full 7-column @@unique candidate name; ACTUAL/HISTORICAL's name (\"legal_corpus_materializations_identity_key\", 42 chars) is a deliberately short, hand-picked name from the table's governing migration, unrelated by truncation to the long form -- both refer to the identical unique index over the same 7 columns.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: legal_corpus_materializations|legal_corpus_materializations_logical_source_id_registry_ar_key",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index legal_corpus_materializations_logical_source_id_registry_ar_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX legal_corpus_materializations_logical_source_id_registry_ar_key ON public.legal_corpus_materializations USING btree (logical_source_id, registry_artifact_id, raw_source_content_hash, text_projection_hash, text_projection_version, corpus_materialization_version, chunk_policy_version)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. DECLARED's name (\"...logical_source_id_registry_ar_key\", 63 chars) is Prisma's standard truncated-to-63 rendering of the full 7-column @@unique candidate name; ACTUAL/HISTORICAL's name (\"legal_corpus_materializations_identity_key\", 42 chars) is a deliberately short, hand-picked name from the table's governing migration, unrelated by truncation to the long form -- both refer to the identical unique index over the same 7 columns.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: legal_corpus_materialized_chunks|legal_corpus_materialized_chunks_materialization_id_fragmen_key",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index legal_corpus_materialized_chunks_materialization_id_fragmen_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX legal_corpus_materialized_chunks_materialization_id_fragmen_key ON public.legal_corpus_materialized_chunks USING btree (materialization_id, fragment_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Full candidate name \"legal_corpus_materialized_chunks_materialization_id_fragment_id_key\" (68 chars) truncates via Postgres's naive left-truncation to the ACTUAL/HISTORICAL name ending \"..._fragment_i\" and via Prisma's base-truncate-then-suffix algorithm to the DECLARED name ending \"..._fragmen_key\" -- both are 63-char truncations of the identical full name, verified by direct string-length inspection (63 and 62 chars respectively, consistent with the two known truncation algorithms).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: legal_corpus_materialized_chunks|legal_corpus_materialized_chunks_materialization_id_fragment_i",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index legal_corpus_materialized_chunks_materialization_id_fragment_i unique=true primary=false predicate=null :: CREATE UNIQUE INDEX legal_corpus_materialized_chunks_materialization_id_fragment_i ON public.legal_corpus_materialized_chunks USING btree (materialization_id, fragment_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index legal_corpus_materialized_chunks_materialization_id_fragment_i unique=true primary=false predicate=null :: CREATE UNIQUE INDEX legal_corpus_materialized_chunks_materialization_id_fragment_i ON public.legal_corpus_materialized_chunks USING btree (materialization_id, fragment_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Full candidate name \"legal_corpus_materialized_chunks_materialization_id_fragment_id_key\" (68 chars) truncates via Postgres's naive left-truncation to the ACTUAL/HISTORICAL name ending \"..._fragment_i\" and via Prisma's base-truncate-then-suffix algorithm to the DECLARED name ending \"..._fragmen_key\" -- both are 63-char truncations of the identical full name, verified by direct string-length inspection (63 and 62 chars respectively, consistent with the two known truncation algorithms).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: legal_corpus_materialized_chunks|legal_corpus_materialized_chunks_materialization_id_sequenc_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index legal_corpus_materialized_chunks_materialization_id_sequenc_idx unique=false primary=false predicate=null :: CREATE INDEX legal_corpus_materialized_chunks_materialization_id_sequenc_idx ON public.legal_corpus_materialized_chunks USING btree (materialization_id, sequence)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same mechanism as the fragment_id pair on the same table: two different 63-byte-limit truncations (\"..._sequenc_idx\" DECLARED vs \"..._sequence_i\" ACTUAL/HISTORICAL) of one full candidate index name over (materialization_id, sequence).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: legal_corpus_materialized_chunks|legal_corpus_materialized_chunks_materialization_id_sequence_i",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index legal_corpus_materialized_chunks_materialization_id_sequence_i unique=false primary=false predicate=null :: CREATE INDEX legal_corpus_materialized_chunks_materialization_id_sequence_i ON public.legal_corpus_materialized_chunks USING btree (materialization_id, sequence)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index legal_corpus_materialized_chunks_materialization_id_sequence_i unique=false primary=false predicate=null :: CREATE INDEX legal_corpus_materialized_chunks_materialization_id_sequence_i ON public.legal_corpus_materialized_chunks USING btree (materialization_id, sequence)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same mechanism as the fragment_id pair on the same table: two different 63-byte-limit truncations (\"..._sequenc_idx\" DECLARED vs \"..._sequence_i\" ACTUAL/HISTORICAL) of one full candidate index name over (materialization_id, sequence).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_geometry_projections|localization_geometry_projections_project_id_geometry_artif_key",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index localization_geometry_projections_project_id_geometry_artif_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX localization_geometry_projections_project_id_geometry_artif_key ON public.localization_geometry_projections USING btree (project_id, geometry_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Both names are exactly 63 characters -- two different truncations of the same full unique-index candidate name over (project_id, geometry_artifact_id): DECLARED truncates the \"artifact_id\" segment to \"artif\" before the \"_key\" suffix; ACTUAL/HISTORICAL is the raw 63-character left-truncation ending in a trailing underscore.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_geometry_projections|localization_geometry_projections_project_id_geometry_artifact_",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index localization_geometry_projections_project_id_geometry_artifact_ unique=true primary=false predicate=null :: CREATE UNIQUE INDEX localization_geometry_projections_project_id_geometry_artifact_ ON public.localization_geometry_projections USING btree (project_id, geometry_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index localization_geometry_projections_project_id_geometry_artifact_ unique=true primary=false predicate=null :: CREATE UNIQUE INDEX localization_geometry_projections_project_id_geometry_artifact_ ON public.localization_geometry_projections USING btree (project_id, geometry_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Both names are exactly 63 characters -- two different truncations of the same full unique-index candidate name over (project_id, geometry_artifact_id): DECLARED truncates the \"artifact_id\" segment to \"artif\" before the \"_key\" suffix; ACTUAL/HISTORICAL is the raw 63-character left-truncation ending in a trailing underscore.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_geometry_supersession_requests|localization_geometry_supersession_requests_project_id_pred_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index localization_geometry_supersession_requests_project_id_pred_idx unique=false primary=false predicate=null :: CREATE INDEX localization_geometry_supersession_requests_project_id_pred_idx ON public.localization_geometry_supersession_requests USING btree (project_id, predecessor_geometry_artifact_id, successor_geometry_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same 3-column index (project_id, predecessor_geometry_artifact_id, successor_geometry_artifact_id) under two different names: DECLARED's \"_project_id_pred_idx\" (63 chars) is Prisma's truncated autogenerated name; ACTUAL/HISTORICAL's \"_subject_idx\" (55 chars) is a short, deliberately-chosen name from the table's governing migration file (prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql, which literally creates \"localization_geometry_supersession_requests_subject_idx\" on exactly these 3 columns).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_geometry_supersession_requests|localization_geometry_supersession_requests_subject_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index localization_geometry_supersession_requests_subject_idx unique=false primary=false predicate=null :: CREATE INDEX localization_geometry_supersession_requests_subject_idx ON public.localization_geometry_supersession_requests USING btree (project_id, predecessor_geometry_artifact_id, successor_geometry_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index localization_geometry_supersession_requests_subject_idx unique=false primary=false predicate=null :: CREATE INDEX localization_geometry_supersession_requests_subject_idx ON public.localization_geometry_supersession_requests USING btree (project_id, predecessor_geometry_artifact_id, successor_geometry_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same 3-column index (project_id, predecessor_geometry_artifact_id, successor_geometry_artifact_id) under two different names: DECLARED's \"_project_id_pred_idx\" (63 chars) is Prisma's truncated autogenerated name; ACTUAL/HISTORICAL's \"_subject_idx\" (55 chars) is a short, deliberately-chosen name from the table's governing migration file (prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql, which literally creates \"localization_geometry_supersession_requests_subject_idx\" on exactly these 3 columns).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_geometry_supersession_requests|localization_geometry_supersession_requests_status_created__idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index localization_geometry_supersession_requests_status_created__idx unique=false primary=false predicate=null :: CREATE INDEX localization_geometry_supersession_requests_status_created__idx ON public.localization_geometry_supersession_requests USING btree (status, created_at)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (status, created_at) index; DECLARED's 63-char name has a double-underscore artifact from base-truncate-then-suffix, ACTUAL/HISTORICAL's 62-char name is confirmed created verbatim by the same governing migration file (20260823160000_add_localization_geometry_supersession/migration.sql).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_geometry_supersession_requests|localization_geometry_supersession_requests_status_created_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index localization_geometry_supersession_requests_status_created_idx unique=false primary=false predicate=null :: CREATE INDEX localization_geometry_supersession_requests_status_created_idx ON public.localization_geometry_supersession_requests USING btree (status, created_at)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index localization_geometry_supersession_requests_status_created_idx unique=false primary=false predicate=null :: CREATE INDEX localization_geometry_supersession_requests_status_created_idx ON public.localization_geometry_supersession_requests USING btree (status, created_at)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (status, created_at) index; DECLARED's 63-char name has a double-underscore artifact from base-truncate-then-suffix, ACTUAL/HISTORICAL's 62-char name is confirmed created verbatim by the same governing migration file (20260823160000_add_localization_geometry_supersession/migration.sql).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_identity_provisioning_requests|localization_identity_provisioning_requests_project_id_geom_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index localization_identity_provisioning_requests_project_id_geom_idx unique=false primary=false predicate=null :: CREATE INDEX localization_identity_provisioning_requests_project_id_geom_idx ON public.localization_identity_provisioning_requests USING btree (project_id, geometry_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, geometry_artifact_id) index; both names are 63 characters, two different truncations of the same full candidate name (DECLARED's \"..._geom_idx\" via base-truncate-then-suffix, ACTUAL/HISTORICAL's \"..._geometry\" via naive left-truncation).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_identity_provisioning_requests|localization_identity_provisioning_requests_project_id_geometry",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index localization_identity_provisioning_requests_project_id_geometry unique=false primary=false predicate=null :: CREATE INDEX localization_identity_provisioning_requests_project_id_geometry ON public.localization_identity_provisioning_requests USING btree (project_id, geometry_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index localization_identity_provisioning_requests_project_id_geometry unique=false primary=false predicate=null :: CREATE INDEX localization_identity_provisioning_requests_project_id_geometry ON public.localization_identity_provisioning_requests USING btree (project_id, geometry_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, geometry_artifact_id) index; both names are 63 characters, two different truncations of the same full candidate name (DECLARED's \"..._geom_idx\" via base-truncate-then-suffix, ACTUAL/HISTORICAL's \"..._geometry\" via naive left-truncation).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_identity_provisioning_requests|localization_identity_provisioning_requests_status_created__idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index localization_identity_provisioning_requests_status_created__idx unique=false primary=false predicate=null :: CREATE INDEX localization_identity_provisioning_requests_status_created__idx ON public.localization_identity_provisioning_requests USING btree (status, created_at)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (status, created_at) index; both names are 63 characters, two different truncations of the same full candidate name.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_identity_provisioning_requests|localization_identity_provisioning_requests_status_created_at_i",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index localization_identity_provisioning_requests_status_created_at_i unique=false primary=false predicate=null :: CREATE INDEX localization_identity_provisioning_requests_status_created_at_i ON public.localization_identity_provisioning_requests USING btree (status, created_at)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index localization_identity_provisioning_requests_status_created_at_i unique=false primary=false predicate=null :: CREATE INDEX localization_identity_provisioning_requests_status_created_at_i ON public.localization_identity_provisioning_requests USING btree (status, created_at)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (status, created_at) index; both names are 63 characters, two different truncations of the same full candidate name.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_assessment_projections|project_assessment_projections_binding_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_assessment_projections_binding_idx unique=false primary=false predicate=null :: CREATE INDEX project_assessment_projections_binding_idx ON public.project_assessment_projections USING btree (project_id, binding_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_assessment_projections_binding_idx unique=false primary=false predicate=null :: CREATE INDEX project_assessment_projections_binding_idx ON public.project_assessment_projections USING btree (project_id, binding_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, binding_artifact_id) index; ACTUAL/HISTORICAL's \"_binding_idx\" (42 chars) is a short hand-picked name from the governing migration, DECLARED's \"_project_id_binding_artifact__idx\" (63 chars) is Prisma's truncated autogenerated form.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_assessment_projections|project_assessment_projections_project_id_binding_artifact__idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_assessment_projections_project_id_binding_artifact__idx unique=false primary=false predicate=null :: CREATE INDEX project_assessment_projections_project_id_binding_artifact__idx ON public.project_assessment_projections USING btree (project_id, binding_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, binding_artifact_id) index; ACTUAL/HISTORICAL's \"_binding_idx\" (42 chars) is a short hand-picked name from the governing migration, DECLARED's \"_project_id_binding_artifact__idx\" (63 chars) is Prisma's truncated autogenerated form.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_assessment_projections|project_assessment_projections_created_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_assessment_projections_created_idx unique=false primary=false predicate=null :: CREATE INDEX project_assessment_projections_created_idx ON public.project_assessment_projections USING btree (project_id, created_at)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_assessment_projections_created_idx unique=false primary=false predicate=null :: CREATE INDEX project_assessment_projections_created_idx ON public.project_assessment_projections USING btree (project_id, created_at)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, created_at) index; ACTUAL/HISTORICAL's short \"_created_idx\" vs DECLARED's longer Prisma-autogenerated \"_project_id_created_at_idx\" (56 chars, under the 63 limit but still a different, longer naming convention).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_assessment_projections|project_assessment_projections_project_id_created_at_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_assessment_projections_project_id_created_at_idx unique=false primary=false predicate=null :: CREATE INDEX project_assessment_projections_project_id_created_at_idx ON public.project_assessment_projections USING btree (project_id, created_at)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, created_at) index; ACTUAL/HISTORICAL's short \"_created_idx\" vs DECLARED's longer Prisma-autogenerated \"_project_id_created_at_idx\" (56 chars, under the 63 limit but still a different, longer naming convention).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_assessment_projections|project_assessment_projections_project_assessment_key",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_assessment_projections_project_assessment_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX project_assessment_projections_project_assessment_key ON public.project_assessment_projections USING btree (project_id, assessment_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_assessment_projections_project_assessment_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX project_assessment_projections_project_assessment_key ON public.project_assessment_projections USING btree (project_id, assessment_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, assessment_artifact_id) unique index; ACTUAL/HISTORICAL's hand-picked \"_project_assessment_key\" (53 chars) vs DECLARED's 63-char Prisma-truncated \"_project_id_assessment_artifa_key\".",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_assessment_projections|project_assessment_projections_project_id_assessment_artifa_key",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_assessment_projections_project_id_assessment_artifa_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX project_assessment_projections_project_id_assessment_artifa_key ON public.project_assessment_projections USING btree (project_id, assessment_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, assessment_artifact_id) unique index; ACTUAL/HISTORICAL's hand-picked \"_project_assessment_key\" (53 chars) vs DECLARED's 63-char Prisma-truncated \"_project_id_assessment_artifa_key\".",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_assessment_projections|project_assessment_projections_project_id_localization_geom_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_assessment_projections_project_id_localization_geom_idx unique=false primary=false predicate=null :: CREATE INDEX project_assessment_projections_project_id_localization_geom_idx ON public.project_assessment_projections USING btree (project_id, localization_geometry_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, localization_geometry_artifact_id) index; both names are 63 characters, two different truncations of the same full candidate name.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_assessment_projections|project_assessment_projections_project_id_localization_geometry",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_assessment_projections_project_id_localization_geometry unique=false primary=false predicate=null :: CREATE INDEX project_assessment_projections_project_id_localization_geometry ON public.project_assessment_projections USING btree (project_id, localization_geometry_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_assessment_projections_project_id_localization_geometry unique=false primary=false predicate=null :: CREATE INDEX project_assessment_projections_project_id_localization_geometry ON public.project_assessment_projections USING btree (project_id, localization_geometry_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, localization_geometry_artifact_id) index; both names are 63 characters, two different truncations of the same full candidate name.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_binding_supersessions|project_context_binding_supersessions_artifact_key",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_context_binding_supersessions_artifact_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX project_context_binding_supersessions_artifact_key ON public.project_context_binding_supersessions USING btree (supersession_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_context_binding_supersessions_artifact_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX project_context_binding_supersessions_artifact_key ON public.project_context_binding_supersessions USING btree (supersession_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same unique index over (supersession_artifact_id); ACTUAL/HISTORICAL's hand-picked \"_artifact_key\" (50 chars) vs DECLARED's full untruncated Prisma name \"_supersession_artifact_key\" (63 chars, at the limit).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_binding_supersessions|project_context_binding_supersessions_supersession_artifact_key",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_context_binding_supersessions_supersession_artifact_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX project_context_binding_supersessions_supersession_artifact_key ON public.project_context_binding_supersessions USING btree (supersession_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same unique index over (supersession_artifact_id); ACTUAL/HISTORICAL's hand-picked \"_artifact_key\" (50 chars) vs DECLARED's full untruncated Prisma name \"_supersession_artifact_key\" (63 chars, at the limit).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_binding_supersessions|project_context_binding_supersessions_predecessor_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_context_binding_supersessions_predecessor_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_binding_supersessions_predecessor_idx ON public.project_context_binding_supersessions USING btree (project_id, superseded_binding_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_context_binding_supersessions_predecessor_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_binding_supersessions_predecessor_idx ON public.project_context_binding_supersessions USING btree (project_id, superseded_binding_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, superseded_binding_artifact_id) index; ACTUAL/HISTORICAL's hand-picked \"_predecessor_idx\" (53 chars) vs DECLARED's longer Prisma-style \"_project_id_superseded_idx\" (63 chars).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_binding_supersessions|project_context_binding_supersessions_project_id_superseded_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_context_binding_supersessions_project_id_superseded_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_binding_supersessions_project_id_superseded_idx ON public.project_context_binding_supersessions USING btree (project_id, superseded_binding_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, superseded_binding_artifact_id) index; ACTUAL/HISTORICAL's hand-picked \"_predecessor_idx\" (53 chars) vs DECLARED's longer Prisma-style \"_project_id_superseded_idx\" (63 chars).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_binding_supersessions|project_context_binding_supersessions_project_id_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_context_binding_supersessions_project_id_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_binding_supersessions_project_id_idx ON public.project_context_binding_supersessions USING btree (project_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same single-column (project_id) index under two conventionally-different but semantically identical names (\"_project_id_idx\" DECLARED, Prisma's standard convention, vs \"_project_idx\" ACTUAL/HISTORICAL, the table's own hand-picked short form).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_binding_supersessions|project_context_binding_supersessions_project_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_context_binding_supersessions_project_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_binding_supersessions_project_idx ON public.project_context_binding_supersessions USING btree (project_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_context_binding_supersessions_project_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_binding_supersessions_project_idx ON public.project_context_binding_supersessions USING btree (project_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same single-column (project_id) index under two conventionally-different but semantically identical names (\"_project_id_idx\" DECLARED, Prisma's standard convention, vs \"_project_idx\" ACTUAL/HISTORICAL, the table's own hand-picked short form).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_binding_supersessions|project_context_binding_supersessions_project_id_successor__idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_context_binding_supersessions_project_id_successor__idx unique=false primary=false predicate=null :: CREATE INDEX project_context_binding_supersessions_project_id_successor__idx ON public.project_context_binding_supersessions USING btree (project_id, successor_binding_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, successor_binding_artifact_id) index; ACTUAL/HISTORICAL's hand-picked \"_successor_idx\" (51 chars) vs DECLARED's 63-char Prisma-truncated \"_project_id_successor__idx\".",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_binding_supersessions|project_context_binding_supersessions_successor_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_context_binding_supersessions_successor_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_binding_supersessions_successor_idx ON public.project_context_binding_supersessions USING btree (project_id, successor_binding_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_context_binding_supersessions_successor_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_binding_supersessions_successor_idx ON public.project_context_binding_supersessions USING btree (project_id, successor_binding_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (project_id, successor_binding_artifact_id) index; ACTUAL/HISTORICAL's hand-picked \"_successor_idx\" (51 chars) vs DECLARED's 63-char Prisma-truncated \"_project_id_successor__idx\".",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_bindings|project_context_bindings_project_context_idx",
      "source_classification": "ACTUAL_ONLY",
      "actual_definition": "index project_context_bindings_project_context_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_bindings_project_context_idx ON public.project_context_bindings USING btree (project_id, project_context_artifact_id, project_context_artifact_type)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same 3-column (project_id, project_context_artifact_id, project_context_artifact_type) non-unique index; ACTUAL is confirmed created verbatim, by name, in prisma/migrations/20260820150000_add_project_context_binding/migration.sql line 25 (\"CREATE INDEX \\\"project_context_bindings_project_context_idx\\\" ...\"); note this migration was NOT captured in the HISTORICAL reconstruction at all despite existing in prisma/migrations (every object from this table shows HISTORICAL null even where ACTUAL and DECLARED otherwise match perfectly elsewhere in the full 1947-item dataset) -- a gap in the frozen HISTORICAL artifact's own reconstruction, not evidence of a real schema difference. DECLARED's name is schema.prisma's `@@index([projectId, projectContextArtifactId, projectContextArtifactType])` (model ProjectContextBinding, no map override) rendered via Prisma's standard truncated convention.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_bindings|project_context_bindings_project_id_project_context_artifac_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_context_bindings_project_id_project_context_artifac_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_bindings_project_id_project_context_artifac_idx ON public.project_context_bindings USING btree (project_id, project_context_artifact_id, project_context_artifact_type)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same 3-column (project_id, project_context_artifact_id, project_context_artifact_type) non-unique index; ACTUAL is confirmed created verbatim, by name, in prisma/migrations/20260820150000_add_project_context_binding/migration.sql line 25 (\"CREATE INDEX \\\"project_context_bindings_project_context_idx\\\" ...\"); note this migration was NOT captured in the HISTORICAL reconstruction at all despite existing in prisma/migrations (every object from this table shows HISTORICAL null even where ACTUAL and DECLARED otherwise match perfectly elsewhere in the full 1947-item dataset) -- a gap in the frozen HISTORICAL artifact's own reconstruction, not evidence of a real schema difference. DECLARED's name is schema.prisma's `@@index([projectId, projectContextArtifactId, projectContextArtifactType])` (model ProjectContextBinding, no map override) rendered via Prisma's standard truncated convention.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_bindings|project_context_bindings_project_context_key",
      "source_classification": "ACTUAL_ONLY",
      "actual_definition": "index project_context_bindings_project_context_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX project_context_bindings_project_context_key ON public.project_context_bindings USING btree (project_id, project_context_artifact_id, project_context_artifact_type)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same 3-column unique index as the sibling _idx pair above, same migration file (line 23), same HISTORICAL-reconstruction gap, same DECLARED-side `@@unique([...])` in schema.prisma.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_bindings|project_context_bindings_project_id_project_context_artifac_key",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_context_bindings_project_id_project_context_artifac_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX project_context_bindings_project_id_project_context_artifac_key ON public.project_context_bindings USING btree (project_id, project_context_artifact_id, project_context_artifact_type)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same 3-column unique index as the sibling _idx pair above, same migration file (line 23), same HISTORICAL-reconstruction gap, same DECLARED-side `@@unique([...])` in schema.prisma.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_bootstrap_requests|project_context_bootstrap_requests_project_id_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_context_bootstrap_requests_project_id_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_bootstrap_requests_project_id_idx ON public.project_context_bootstrap_requests USING btree (project_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same single-column (project_id) index; ACTUAL/HISTORICAL's hand-picked \"_project_idx\" (46 chars) vs DECLARED's Prisma-standard \"_project_id_idx\" (49 chars).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_bootstrap_requests|project_context_bootstrap_requests_project_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_context_bootstrap_requests_project_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_bootstrap_requests_project_idx ON public.project_context_bootstrap_requests USING btree (project_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_context_bootstrap_requests_project_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_bootstrap_requests_project_idx ON public.project_context_bootstrap_requests USING btree (project_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same single-column (project_id) index; ACTUAL/HISTORICAL's hand-picked \"_project_idx\" (46 chars) vs DECLARED's Prisma-standard \"_project_id_idx\" (49 chars).",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_bootstrap_requests|project_context_bootstrap_requests_status_created_at_idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index project_context_bootstrap_requests_status_created_at_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_bootstrap_requests_status_created_at_idx ON public.project_context_bootstrap_requests USING btree (status, created_at)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (status, created_at) index; ACTUAL/HISTORICAL's \"_status_created_idx\" (53 chars) vs DECLARED's \"_status_created_at_idx\" (56 chars) -- both well under the 63-byte limit, simply two different naming choices for the same columns.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: project_context_bootstrap_requests|project_context_bootstrap_requests_status_created_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index project_context_bootstrap_requests_status_created_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_bootstrap_requests_status_created_idx ON public.project_context_bootstrap_requests USING btree (status, created_at)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index project_context_bootstrap_requests_status_created_idx unique=false primary=false predicate=null :: CREATE INDEX project_context_bootstrap_requests_status_created_idx ON public.project_context_bootstrap_requests USING btree (status, created_at)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same (status, created_at) index; ACTUAL/HISTORICAL's \"_status_created_idx\" (53 chars) vs DECLARED's \"_status_created_at_idx\" (56 chars) -- both well under the 63-byte limit, simply two different naming choices for the same columns.",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: viewer_capability_provisioning_requests|viewer_capability_provisioning_requests_project_id_context__idx",
      "source_classification": "DECLARED_ONLY",
      "actual_definition": "ABSENT",
      "declared_definition": "index viewer_capability_provisioning_requests_project_id_context__idx unique=false primary=false predicate=null :: CREATE INDEX viewer_capability_provisioning_requests_project_id_context__idx ON public.viewer_capability_provisioning_requests USING btree (project_id, context_binding_artifact_id, release_artifact_id, viewer_identity_artifact_id)",
      "historical_definition_for_reference": "ABSENT",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same 4-column (project_id, context_binding_artifact_id, release_artifact_id, viewer_identity_artifact_id) index; both names are exactly 63 characters, two different truncations/abbreviations of the same long candidate name (DECLARED keeps full segment names truncated at the column boundary, ACTUAL/HISTORICAL abbreviates each column to a short token \"ctx_rel_vid\").",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: viewer_capability_provisioning_requests|viewer_capability_provisioning_requests_project_id_ctx_rel_vid_",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index viewer_capability_provisioning_requests_project_id_ctx_rel_vid_ unique=false primary=false predicate=null :: CREATE INDEX viewer_capability_provisioning_requests_project_id_ctx_rel_vid_ ON public.viewer_capability_provisioning_requests USING btree (project_id, context_binding_artifact_id, release_artifact_id, viewer_identity_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index viewer_capability_provisioning_requests_project_id_ctx_rel_vid_ unique=false primary=false predicate=null :: CREATE INDEX viewer_capability_provisioning_requests_project_id_ctx_rel_vid_ ON public.viewer_capability_provisioning_requests USING btree (project_id, context_binding_artifact_id, release_artifact_id, viewer_identity_artifact_id)",
      "evidence": "Definition strings compared with the CREATE [UNIQUE] INDEX <name> ON prefix stripped: identical on both sides (verified programmatically across all 21 such pairs in this table). See table-specific note for the naming mechanism.",
      "disposition": "NAME_TRUNCATION_EQUIVALENT",
      "reason": "Column list, uniqueness, access method and predicate are byte-identical between the ACTUAL/HISTORICAL-side object and the DECLARED-side object once the index name itself is normalized out of the definition string; only the identifier differs. Same 4-column (project_id, context_binding_artifact_id, release_artifact_id, viewer_identity_artifact_id) index; both names are exactly 63 characters, two different truncations/abbreviations of the same long candidate name (DECLARED keeps full segment names truncated at the column boundary, ACTUAL/HISTORICAL abbreviates each column to a short token \"ctx_rel_vid\").",
      "residual_uncertainty": "Low -- identical column/uniqueness/predicate is a strong, mechanical proof of same-object status regardless of why the identifier differs."
    },
    {
      "object_identity": "indexes: localization_geometry_supersessions|localization_geometry_supersessions_pkey",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index localization_geometry_supersessions_pkey unique=true primary=true predicate=null :: CREATE UNIQUE INDEX localization_geometry_supersessions_pkey ON public.localization_geometry_supersessions USING btree (id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index localization_geometry_supersessions_pkey unique=true primary=true predicate=null :: CREATE UNIQUE INDEX localization_geometry_supersessions_pkey ON public.localization_geometry_supersessions USING btree (id)",
      "evidence": "Index of the documented raw-SQL-only projection table localization_geometry_supersessions -- see prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment (\"...raw SQL access, not a Prisma model\") and confirmed absence of any schema.prisma model mapping to this table name.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Index of the documented raw-SQL-only projection table localization_geometry_supersessions.",
      "residual_uncertainty": "Low."
    },
    {
      "object_identity": "indexes: localization_geometry_supersessions|localization_geometry_supersessions_project_idx",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index localization_geometry_supersessions_project_idx unique=false primary=false predicate=null :: CREATE INDEX localization_geometry_supersessions_project_idx ON public.localization_geometry_supersessions USING btree (project_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index localization_geometry_supersessions_project_idx unique=false primary=false predicate=null :: CREATE INDEX localization_geometry_supersessions_project_idx ON public.localization_geometry_supersessions USING btree (project_id)",
      "evidence": "Index of the documented raw-SQL-only projection table localization_geometry_supersessions -- see prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment (\"...raw SQL access, not a Prisma model\") and confirmed absence of any schema.prisma model mapping to this table name.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Index of the documented raw-SQL-only projection table localization_geometry_supersessions.",
      "residual_uncertainty": "Low."
    },
    {
      "object_identity": "indexes: localization_geometry_supersessions|localization_geometry_supersessions_project_supersession_key",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "index localization_geometry_supersessions_project_supersession_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX localization_geometry_supersessions_project_supersession_key ON public.localization_geometry_supersessions USING btree (project_id, supersession_artifact_id)",
      "declared_definition": "ABSENT",
      "historical_definition_for_reference": "index localization_geometry_supersessions_project_supersession_key unique=true primary=false predicate=null :: CREATE UNIQUE INDEX localization_geometry_supersessions_project_supersession_key ON public.localization_geometry_supersessions USING btree (project_id, supersession_artifact_id)",
      "evidence": "Index of the documented raw-SQL-only projection table localization_geometry_supersessions -- see prisma/migrations/20260823160000_add_localization_geometry_supersession/migration.sql header comment (\"...raw SQL access, not a Prisma model\") and confirmed absence of any schema.prisma model mapping to this table name.",
      "disposition": "COMPARISON_ARTIFACT",
      "reason": "Index of the documented raw-SQL-only projection table localization_geometry_supersessions.",
      "residual_uncertainty": "Low."
    },
    {
      "object_identity": "enums: ConfidenceLevel|HIGH",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "enum ConfidenceLevel value=HIGH sort_order=3",
      "declared_definition": "enum ConfidenceLevel value=HIGH sort_order=1",
      "historical_definition_for_reference": "enum ConfidenceLevel value=HIGH sort_order=3",
      "evidence": "actual_fingerprint === historical_fingerprint (byte-identical) while declared_fingerprint differs, for this enum value; sort_order fields quoted directly from file 1's structured actual/declared/historical objects (3 vs 1 vs 3).",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "The value set is not in dispute (this exact value exists identically on all three sides); only its ORDINAL POSITION within the enum differs: ACTUAL sort_order=3, DECLARED sort_order=1, HISTORICAL sort_order=3. ACTUAL and HISTORICAL agree exactly (the production enum's internal Postgres ordering matches what was actually migrated); only schema.prisma's textual ordering of this enum's values has drifted from that -- most likely someone reordered the values in the .prisma file for readability without an accompanying ALTER TYPE ... ADD VALUE ... AFTER migration to physically reorder the Postgres enum (which cannot be done with a simple statement). Per this review's hard rule that enum order is a checkable property distinct from the value set, this is a real, evidenced difference.",
      "residual_uncertainty": "Low on the fact of the ordering mismatch (directly evidenced by the sort_order fields); moderate on downstream consequence -- Postgres enum order only matters where application code relies on default enum comparison/ORDER BY semantics rather than an explicit CASE/mapping, which was not separately verified in server/ as part of this schema-only pass."
    },
    {
      "object_identity": "enums: ConfidenceLevel|LOW",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "enum ConfidenceLevel value=LOW sort_order=1",
      "declared_definition": "enum ConfidenceLevel value=LOW sort_order=3",
      "historical_definition_for_reference": "enum ConfidenceLevel value=LOW sort_order=1",
      "evidence": "actual_fingerprint === historical_fingerprint (byte-identical) while declared_fingerprint differs, for this enum value; sort_order fields quoted directly from file 1's structured actual/declared/historical objects (1 vs 3 vs 1).",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "The value set is not in dispute (this exact value exists identically on all three sides); only its ORDINAL POSITION within the enum differs: ACTUAL sort_order=1, DECLARED sort_order=3, HISTORICAL sort_order=1. ACTUAL and HISTORICAL agree exactly (the production enum's internal Postgres ordering matches what was actually migrated); only schema.prisma's textual ordering of this enum's values has drifted from that -- most likely someone reordered the values in the .prisma file for readability without an accompanying ALTER TYPE ... ADD VALUE ... AFTER migration to physically reorder the Postgres enum (which cannot be done with a simple statement). Per this review's hard rule that enum order is a checkable property distinct from the value set, this is a real, evidenced difference.",
      "residual_uncertainty": "Low on the fact of the ordering mismatch (directly evidenced by the sort_order fields); moderate on downstream consequence -- Postgres enum order only matters where application code relies on default enum comparison/ORDER BY semantics rather than an explicit CASE/mapping, which was not separately verified in server/ as part of this schema-only pass."
    },
    {
      "object_identity": "enums: DocumentProcessingStatus|CHUNKED",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "enum DocumentProcessingStatus value=CHUNKED sort_order=5",
      "declared_definition": "enum DocumentProcessingStatus value=CHUNKED sort_order=3",
      "historical_definition_for_reference": "enum DocumentProcessingStatus value=CHUNKED sort_order=5",
      "evidence": "actual_fingerprint === historical_fingerprint (byte-identical) while declared_fingerprint differs, for this enum value; sort_order fields quoted directly from file 1's structured actual/declared/historical objects (5 vs 3 vs 5).",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "The value set is not in dispute (this exact value exists identically on all three sides); only its ORDINAL POSITION within the enum differs: ACTUAL sort_order=5, DECLARED sort_order=3, HISTORICAL sort_order=5. ACTUAL and HISTORICAL agree exactly (the production enum's internal Postgres ordering matches what was actually migrated); only schema.prisma's textual ordering of this enum's values has drifted from that -- most likely someone reordered the values in the .prisma file for readability without an accompanying ALTER TYPE ... ADD VALUE ... AFTER migration to physically reorder the Postgres enum (which cannot be done with a simple statement). Per this review's hard rule that enum order is a checkable property distinct from the value set, this is a real, evidenced difference.",
      "residual_uncertainty": "Low on the fact of the ordering mismatch (directly evidenced by the sort_order fields); moderate on downstream consequence -- Postgres enum order only matters where application code relies on default enum comparison/ORDER BY semantics rather than an explicit CASE/mapping, which was not separately verified in server/ as part of this schema-only pass."
    },
    {
      "object_identity": "enums: DocumentProcessingStatus|EMBEDDED",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "enum DocumentProcessingStatus value=EMBEDDED sort_order=3",
      "declared_definition": "enum DocumentProcessingStatus value=EMBEDDED sort_order=4",
      "historical_definition_for_reference": "enum DocumentProcessingStatus value=EMBEDDED sort_order=3",
      "evidence": "actual_fingerprint === historical_fingerprint (byte-identical) while declared_fingerprint differs, for this enum value; sort_order fields quoted directly from file 1's structured actual/declared/historical objects (3 vs 4 vs 3).",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "The value set is not in dispute (this exact value exists identically on all three sides); only its ORDINAL POSITION within the enum differs: ACTUAL sort_order=3, DECLARED sort_order=4, HISTORICAL sort_order=3. ACTUAL and HISTORICAL agree exactly (the production enum's internal Postgres ordering matches what was actually migrated); only schema.prisma's textual ordering of this enum's values has drifted from that -- most likely someone reordered the values in the .prisma file for readability without an accompanying ALTER TYPE ... ADD VALUE ... AFTER migration to physically reorder the Postgres enum (which cannot be done with a simple statement). Per this review's hard rule that enum order is a checkable property distinct from the value set, this is a real, evidenced difference.",
      "residual_uncertainty": "Low on the fact of the ordering mismatch (directly evidenced by the sort_order fields); moderate on downstream consequence -- Postgres enum order only matters where application code relies on default enum comparison/ORDER BY semantics rather than an explicit CASE/mapping, which was not separately verified in server/ as part of this schema-only pass."
    },
    {
      "object_identity": "enums: DocumentProcessingStatus|FAILED",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "enum DocumentProcessingStatus value=FAILED sort_order=4",
      "declared_definition": "enum DocumentProcessingStatus value=FAILED sort_order=5",
      "historical_definition_for_reference": "enum DocumentProcessingStatus value=FAILED sort_order=4",
      "evidence": "actual_fingerprint === historical_fingerprint (byte-identical) while declared_fingerprint differs, for this enum value; sort_order fields quoted directly from file 1's structured actual/declared/historical objects (4 vs 5 vs 4).",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "The value set is not in dispute (this exact value exists identically on all three sides); only its ORDINAL POSITION within the enum differs: ACTUAL sort_order=4, DECLARED sort_order=5, HISTORICAL sort_order=4. ACTUAL and HISTORICAL agree exactly (the production enum's internal Postgres ordering matches what was actually migrated); only schema.prisma's textual ordering of this enum's values has drifted from that -- most likely someone reordered the values in the .prisma file for readability without an accompanying ALTER TYPE ... ADD VALUE ... AFTER migration to physically reorder the Postgres enum (which cannot be done with a simple statement). Per this review's hard rule that enum order is a checkable property distinct from the value set, this is a real, evidenced difference.",
      "residual_uncertainty": "Low on the fact of the ordering mismatch (directly evidenced by the sort_order fields); moderate on downstream consequence -- Postgres enum order only matters where application code relies on default enum comparison/ORDER BY semantics rather than an explicit CASE/mapping, which was not separately verified in server/ as part of this schema-only pass."
    },
    {
      "object_identity": "enums: RequirementVerificationStatus|NEEDS_REVIEW",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "enum RequirementVerificationStatus value=NEEDS_REVIEW sort_order=5",
      "declared_definition": "enum RequirementVerificationStatus value=NEEDS_REVIEW sort_order=2",
      "historical_definition_for_reference": "enum RequirementVerificationStatus value=NEEDS_REVIEW sort_order=5",
      "evidence": "actual_fingerprint === historical_fingerprint (byte-identical) while declared_fingerprint differs, for this enum value; sort_order fields quoted directly from file 1's structured actual/declared/historical objects (5 vs 2 vs 5).",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "The value set is not in dispute (this exact value exists identically on all three sides); only its ORDINAL POSITION within the enum differs: ACTUAL sort_order=5, DECLARED sort_order=2, HISTORICAL sort_order=5. ACTUAL and HISTORICAL agree exactly (the production enum's internal Postgres ordering matches what was actually migrated); only schema.prisma's textual ordering of this enum's values has drifted from that -- most likely someone reordered the values in the .prisma file for readability without an accompanying ALTER TYPE ... ADD VALUE ... AFTER migration to physically reorder the Postgres enum (which cannot be done with a simple statement). Per this review's hard rule that enum order is a checkable property distinct from the value set, this is a real, evidenced difference.",
      "residual_uncertainty": "Low on the fact of the ordering mismatch (directly evidenced by the sort_order fields); moderate on downstream consequence -- Postgres enum order only matters where application code relies on default enum comparison/ORDER BY semantics rather than an explicit CASE/mapping, which was not separately verified in server/ as part of this schema-only pass."
    },
    {
      "object_identity": "enums: RequirementVerificationStatus|REJECTED",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "enum RequirementVerificationStatus value=REJECTED sort_order=4",
      "declared_definition": "enum RequirementVerificationStatus value=REJECTED sort_order=5",
      "historical_definition_for_reference": "enum RequirementVerificationStatus value=REJECTED sort_order=4",
      "evidence": "actual_fingerprint === historical_fingerprint (byte-identical) while declared_fingerprint differs, for this enum value; sort_order fields quoted directly from file 1's structured actual/declared/historical objects (4 vs 5 vs 4).",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "The value set is not in dispute (this exact value exists identically on all three sides); only its ORDINAL POSITION within the enum differs: ACTUAL sort_order=4, DECLARED sort_order=5, HISTORICAL sort_order=4. ACTUAL and HISTORICAL agree exactly (the production enum's internal Postgres ordering matches what was actually migrated); only schema.prisma's textual ordering of this enum's values has drifted from that -- most likely someone reordered the values in the .prisma file for readability without an accompanying ALTER TYPE ... ADD VALUE ... AFTER migration to physically reorder the Postgres enum (which cannot be done with a simple statement). Per this review's hard rule that enum order is a checkable property distinct from the value set, this is a real, evidenced difference.",
      "residual_uncertainty": "Low on the fact of the ordering mismatch (directly evidenced by the sort_order fields); moderate on downstream consequence -- Postgres enum order only matters where application code relies on default enum comparison/ORDER BY semantics rather than an explicit CASE/mapping, which was not separately verified in server/ as part of this schema-only pass."
    },
    {
      "object_identity": "enums: RequirementVerificationStatus|REVIEWED",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "enum RequirementVerificationStatus value=REVIEWED sort_order=2",
      "declared_definition": "enum RequirementVerificationStatus value=REVIEWED sort_order=3",
      "historical_definition_for_reference": "enum RequirementVerificationStatus value=REVIEWED sort_order=2",
      "evidence": "actual_fingerprint === historical_fingerprint (byte-identical) while declared_fingerprint differs, for this enum value; sort_order fields quoted directly from file 1's structured actual/declared/historical objects (2 vs 3 vs 2).",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "The value set is not in dispute (this exact value exists identically on all three sides); only its ORDINAL POSITION within the enum differs: ACTUAL sort_order=2, DECLARED sort_order=3, HISTORICAL sort_order=2. ACTUAL and HISTORICAL agree exactly (the production enum's internal Postgres ordering matches what was actually migrated); only schema.prisma's textual ordering of this enum's values has drifted from that -- most likely someone reordered the values in the .prisma file for readability without an accompanying ALTER TYPE ... ADD VALUE ... AFTER migration to physically reorder the Postgres enum (which cannot be done with a simple statement). Per this review's hard rule that enum order is a checkable property distinct from the value set, this is a real, evidenced difference.",
      "residual_uncertainty": "Low on the fact of the ordering mismatch (directly evidenced by the sort_order fields); moderate on downstream consequence -- Postgres enum order only matters where application code relies on default enum comparison/ORDER BY semantics rather than an explicit CASE/mapping, which was not separately verified in server/ as part of this schema-only pass."
    },
    {
      "object_identity": "enums: RequirementVerificationStatus|VERIFIED",
      "source_classification": "DECLARED_DRIFT",
      "actual_definition": "enum RequirementVerificationStatus value=VERIFIED sort_order=3",
      "declared_definition": "enum RequirementVerificationStatus value=VERIFIED sort_order=4",
      "historical_definition_for_reference": "enum RequirementVerificationStatus value=VERIFIED sort_order=3",
      "evidence": "actual_fingerprint === historical_fingerprint (byte-identical) while declared_fingerprint differs, for this enum value; sort_order fields quoted directly from file 1's structured actual/declared/historical objects (3 vs 4 vs 3).",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "The value set is not in dispute (this exact value exists identically on all three sides); only its ORDINAL POSITION within the enum differs: ACTUAL sort_order=3, DECLARED sort_order=4, HISTORICAL sort_order=3. ACTUAL and HISTORICAL agree exactly (the production enum's internal Postgres ordering matches what was actually migrated); only schema.prisma's textual ordering of this enum's values has drifted from that -- most likely someone reordered the values in the .prisma file for readability without an accompanying ALTER TYPE ... ADD VALUE ... AFTER migration to physically reorder the Postgres enum (which cannot be done with a simple statement). Per this review's hard rule that enum order is a checkable property distinct from the value set, this is a real, evidenced difference.",
      "residual_uncertainty": "Low on the fact of the ordering mismatch (directly evidenced by the sort_order fields); moderate on downstream consequence -- Postgres enum order only matters where application code relies on default enum comparison/ORDER BY semantics rather than an explicit CASE/mapping, which was not separately verified in server/ as part of this schema-only pass."
    },
    {
      "object_identity": "extensions: vector",
      "source_classification": "ACTUAL_DRIFT",
      "actual_definition": "extension vector schema=public version=0.8.6",
      "declared_definition": "extension vector schema=public version=0.8.2",
      "historical_definition_for_reference": "extension vector schema=public version=0.8.2",
      "evidence": "File 1 structured extension objects quoted directly: actual={name:\"vector\",schema:\"public\",version:\"0.8.6\"}; declared={name:\"vector\",schema:\"public\",version:\"0.8.2\"}; historical={name:\"vector\",schema:\"public\",version:\"0.8.2\"}.",
      "disposition": "TRUE_SCHEMA_DRIFT",
      "reason": "ACTUAL has pgvector extension version 0.8.6 installed; DECLARED and HISTORICAL both specify 0.8.2. DECLARED and HISTORICAL agree with each other, meaning schema.prisma's expected version has not changed since the original migration -- ACTUAL alone has drifted, i.e. the production database's pgvector extension was upgraded (via ALTER EXTENSION or a package upgrade) without a corresponding migration or schema.prisma update. Per this review's explicit instruction, an extension version difference must never be normalized away regardless of apparent minorness -- 0.8.2 to 0.8.6 is a real, meaningful upstream version change that can carry behavioral differences (pgvector has changed HNSW/IVFFlat defaults and fixed bugs across patch releases) and directly affects reproducibility of a from-scratch environment build.",
      "residual_uncertainty": "Low on the fact of the version mismatch (directly evidenced); moderate on remediation -- whether to bump declared to 0.8.6 (accept prod's newer version as canonical) or downgrade prod to 0.8.2 is a decision this pass does not make."
    }
  ]
}